# V6-03 执行证据：Course detail 与安全公开 citation

## 状态与基线

- 状态：安全复核的两个 P1 已在本未提交工作树完成 RED→GREEN 并复验；等待主 Agent 创建 `feat(learning): save safe public course citations`；未执行 `git add` 或 `git commit`。
- 分支：`codex/product-prd`。
- 前置提交：`4b4e2186aeeb2b280608eee71375dc32234cc174`（V6-02）。
- 规则：没有真实 Vision/Search/DeepSeek、真实 socket、真实凭据、依赖安装或用户 `EV_DATA_DIR` 访问。

## RED → GREEN

冻结计划 Step 1 的三条 focused 命令先各执行一次 RED：Contracts 缺 V6-03 schemas，Core 缺 fetcher，Web 缺 Course detail component。随后以最小实现达到 GREEN。审阅期间还执行了三条定向 RED：IPv6 publisher 括号、不可序列化 provider output 的 500、UI 在 terminal 422 后滞留 `SEARCHING`；均已由相应 GREEN 测试关闭。安全复核的 P1 又触发了两条可观察 RED：特殊用途 IPv6 实际进入 transport（`undefined.catch`），以及缺少可注入的 absolute header deadline factory；均由明确 deny prefix 和注入式 timer transport 关闭，未打开真实 socket 或等待 5 秒。

## 断言映射

| 冻结边界 | 自动证据 |
| --- | --- |
| strict search input/result、HTTPS URL、unknown field、metadata-only citation | `packages/contracts/tests/courses.test.ts`；结果含 HTML/normalized text 或 provider URL 注入均被 schema 拒绝。 |
| Course detail：Owner、Rule、官方/用户/公开来源、progress、action counts | `apps/core/tests/learning.test.ts`；真实 Rule 与版本化 context 聚合。 |
| Owner isolation | 同一 Core 测试向 Course GET、context PATCH、search create/read/execute 使用第二 Owner；全部 404 且响应不含私有 title/run ID。 |
| absent production Search | 同一 Core 测试：run 是 `BLOCKED_PROVIDER`，execute 为 503 `SEARCH_PROVIDER_NOT_CONFIGURED`；没有 fake output。 |
| disclosure、partial safe result、zero safe result、immutable metadata | 同一 Core 测试：安全结果只保存 citation metadata，unsafe port 被拒；零安全结果为 `FAILED/NO_SAFE_PUBLIC_RESULTS` 且 public resource/citation 均为 0；SQLite 无 body/html/headers/normalized text 列且 citation update 触发 immutable。 |
| provider/file 工作在 SQLite write transaction 外 | 同一 Core 测试的 observer 在 capability search 和每个 fetch 都记录 `false`。 |
| HTTPS/credential/port/SSRF/DNS pinning | `apps/core/tests/public-resource-fetcher.test.ts`：HTTP、credential、非 443、private IPv4/IPv6、IPv4-mapped-private、`.local` 传输前拒绝；mixed DNS 拒绝；每一个 request 的 lookup 只取得当前 hop 的 pinned public IP。 |
| IPv6 special-use fail closed、public IPv6 正例 | 同一 fetcher 测试：discard-only `100::/64`、dummy `100:0:0:1::/64`、translation `64:ff9b::/96`、IETF `2001::/23`、benchmark、ORCHID/ORCHIDv2、AS112、documentation、6to4、former 6bone、SRv6 均在 transport 前拒绝；`2606:4700:4700::1111` 保持可用且 publisher 无方括号。 |
| redirect/encoding/size/type/charset/absolute timeout/injection | 同一 fetcher 测试：每跳 URL/DNS 重验、3 redirect 上限（第 4 个请求拒绝）、gzip、非 HTML/text、非 UTF-8、声明/stream 超 1 MiB 均 fail closed；注入 timer 证明 5 秒 absolute header deadline 独立于 socket inactivity，headers 回调只取消该 timer、15 秒 body deadline 继续存在，timeout destroy/错误只 settle 一次并清理两条 timer；提示注入文本只是 normalized 未渲染数据。 |
| provider malformed output terminality | `apps/core/tests/learning.test.ts`：循环结果返回 422 `SEARCH_RESPONSE_INVALID`，run 终态 `FAILED`，没有 500/stuck `SEARCHING`。 |
| Course Web、真实 PATCH/execute payload、blocked/terminal UI | `apps/web/tests/course-detail-workspace.test.tsx`：三种 source、versioned context body、disclosure 后 execution、Header Idempotency-Key、`BLOCKED_PROVIDER` 无 execute、422 details 还原 `FAILED`。`learning-workspace` 证明 Course list link。 |

## 最终验证

```powershell
npm test --workspace @ev/contracts -- tests/courses.test.ts tests/providers.test.ts
# PASS — 2 files / 19 tests
npm test --workspace @ev/core -- tests/learning.test.ts tests/public-resource-fetcher.test.ts
# PASS — 2 files / 11 tests
npm test --workspace @ev/web -- tests/learning-workspace.test.tsx tests/course-detail-workspace.test.tsx
# PASS — 2 files / 4 tests
npm run typecheck --workspace @ev/contracts
npm run typecheck --workspace @ev/core
npm run typecheck --workspace @ev/web
# all PASS
npx eslint packages/contracts/src/courses.ts packages/contracts/src/providers.ts packages/contracts/tests/courses.test.ts packages/contracts/tests/providers.test.ts apps/core/src/app.ts apps/core/src/modules/learning/public-url-policy.ts apps/core/src/modules/learning/public-resource-fetcher.ts apps/core/src/modules/learning/repository.ts apps/core/src/modules/learning/service.ts apps/core/src/modules/learning/routes.ts apps/core/src/modules/providers/capability-run-repository.ts apps/core/tests/learning.test.ts apps/core/tests/public-resource-fetcher.test.ts apps/web/src/components/learning/learning-workspace.tsx apps/web/src/components/learning/course-detail-workspace.tsx "apps/web/src/app/(dashboard)/courses/[id]/page.tsx" apps/web/tests/learning-workspace.test.tsx apps/web/tests/course-detail-workspace.test.tsx
# PASS
git diff --check
# PASS; only Git LF→CRLF advisory for pre-existing line-ending policy
```

## 审阅结论

- P0=0。
- P1=0（安全复核的 IPv6 special-use 与 absolute header deadline P1 已清零）。
- P2：真实 Search/Vision/DeepSeek provider smoke 为 `NOT RUN — APPROVAL REQUIRED`；生产 absent provider 行为已自动验证为 fail-closed。
- 真实网络证据债不阻塞本任务工程交接，且不得由自动测试或本 Agent 代替执行。
