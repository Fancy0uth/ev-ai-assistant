# V6-01 执行证据：安全本地课表 artifact

## 范围

- Raw `POST /v1/course-artifacts`：BFF/Core 双层 5,000,000-byte 边界，PNG/JPEG/WebP MIME↔magic、尺寸、像素和 SHA-256 验证。
- Owner-scoped private artifact metadata、hash replay 与 `ACTIVE → DELETE_PENDING → DELETED` 文件/SQLite 分离生命周期。
- 本地 `POST /v1/course-imports` preflight、`GET /v1/provider-capabilities`、无 Vision Provider 的 `BLOCKED_PROVIDER` disclosure 与三重 Fake gate。
- Additive-only migration v19：artifact、capability run、import/revision/course/citation/learning/audit lineage；未修改旧数据或历史 `app_version`。

## RED

初始冻结命令已启动三套 V6-01 focused tests；执行环境的并行会话只回传了 Vitest 启动行，未回传失败明细。其明确 RED 缺口是 Base64 import schema、缺失 capability matrix / v19 / raw parser / BFF cap / artifact routes。随后新增 capability lease test 的单独 RED 已完整记录：

```text
npm test --workspace @ev/core -- tests/v0.6-capability-gate.test.ts
FAIL Cannot find module ../src/modules/providers/capability-run-repository
```

这证明当时缺少计划规定的 capability-run storage module；实现后同一测试转绿。

## GREEN 与检查

| 命令 | 结果 |
| --- | --- |
| `npm test --workspace @ev/contracts -- tests/courses.test.ts tests/providers.test.ts` | PASS，2 files / 16 tests |
| `npm test --workspace @ev/core -- tests/course-import.test.ts tests/migrations.test.ts tests/v0.5-reliability-storage.test.ts tests/v0.6-capability-gate.test.ts` | PASS，4 files / 13 tests |
| `npm test --workspace @ev/web -- tests/core-client.test.ts tests/core-bff-idempotency.test.ts tests/schedule-workspace.test.tsx tests/provider-settings.test.tsx` | PASS，4 files / 17 tests |
| `npm run typecheck --workspace @ev/contracts` | PASS |
| `npm run typecheck --workspace @ev/core` | PASS |
| `npm run typecheck --workspace @ev/web` | PASS |
| Task-1 focused `npx eslint …` | PASS |
| `git diff --check` | PASS（仅 CRLF advisory） |

## 修复轮

1. capability matrix 测试只提供一个条目，而响应契约固定三种 capability；将 fixture 补齐三项后通过。
2. JPEG magic 被误先解析为不完整 JPEG、而非判断为 declared-PNG mismatch；先识别实际 magic 再解析尺寸后通过。

## 未触及 / 证据类别

- 未调用 Vision、Search、DeepSeek 或任何真实网络/付费 Provider；真实证据为 `NOT RUN — APPROVAL REQUIRED`。
- 未安装依赖、未访问真实 `EV_DATA_DIR`、未改 Daily Plan 业务语义、未创建 v20、未改写历史 `app_version`。
- 自动化中的 fake 仅覆盖 gate 行为；其 evidence kind 为 `AUTOMATED_FAKE`，不代表真实能力成功。

## 提交阻断

预提交 `git add` 失败：`fatal: Unable to create '.../.git/worktrees/product-prd/index.lock': Permission denied`。未重试或绕过该 Git 权限；所有已验证改动保持在工作树，等待主 Agent 以计划规定的 commit message 创建 V6-01 原子提交。此提交前不启动 V6-02。
