# V6-02 执行证据

## RED / GREEN

- 初始 RED（仅一次）：严格 revision/courseId 被 Contracts 拒绝，extract 路由为 404，Web 没有披露后提取闸门。
- 本轮 RED：confirm 相同 key/hash 返回泛化 409；外部调用没有事务观测；候选 UI 没有编辑/排除控件；跨 Owner 夹具揭示单 Owner check 后用临时 adversarial Owner 固定 404 行为。
- 最终 GREEN：Contracts 7/7、Core 15/15、Web 25/25。

## P1 逐项映射（均已关闭）

| P1 行为 | 可观察证据 |
| --- | --- |
| 候选审阅 | `schedule-workspace.test.tsx` 显示 overall/field confidence、provenance、编辑/包含控件；发送真实 edit/exclude revision payload；保存前无 confirm、保存后启用。 |
| Revision | `course-import.test.ts` 证明 OWNER_EDIT exclusion provenance、immutable trigger、stale version 与 Owner-scoped 读取。 |
| Confirm 幂等 | 同测试证明同 key/hash 回放不复制 Course/entity、同 key 不同 hash 为 `IDEMPOTENCY_KEY_REUSED`、不同 key 为 `IMPORT_ALREADY_CONFIRMED`。 |
| Proposal 接受/拒绝 | ACCEPT 回放仅展开一次并令 import `CONFIRMED`、Today 含 `courseId`；`proposal-api.test.ts` 以真实路由证明 REJECT 后 `course_imports_v2.status=SCHEDULE_REJECTED`、Day 与 Today 都是 0 Event。 |
| Rule 原子性 | `proposal-api.test.ts` 对不存在/失配 Rule 断言 `422 PROPOSAL_CANNOT_APPLY`；事务回滚后 Proposal 仍为 `PENDING/v1`，Day 保持 0 Event。 |
| Owner 不可推断 | `course-import.test.ts` 临时第二 Owner 对 GET/extract/revision/confirm 都是 404，不泄露 import 存在性。 |
| 事务边界 | `course-import.test.ts` 注入 observer，在 artifact read 与 Vision fake 调用时断言同连接 `database.inTransaction === false`。 |

## 门禁与证据债

- 冻结 focused 命令与 Contracts/Core/Web 三个 typecheck、聚焦 eslint 都 exit 0；`git diff --check` 见本任务最终检查。
- P0=0，P1=0。
- 自动 Fake 同时要求 `NODE_ENV=test`、显式 flag 与 runner-owned root；真实 Vision：`NOT RUN — APPROVAL REQUIRED`。
