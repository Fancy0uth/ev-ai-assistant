# V6-04 执行报告：引用学习建议、排程与 0.6.0

日期：2026-08-31（Asia/Shanghai）
基线：`b99fdce feat(learning): save safe public course citations`
提交：未创建；按授权等待主 Agent 创建 `feat(learning): schedule cited study actions`。

## TDD 与 Sol 返回修复

- RED：Core health/capability-run 断言仍为 `0.5.0`，运行 `npm test --workspace @ev/core -- tests/learning.test.ts tests/health.test.ts` 得到 0.6.0 预期失败；最小版本同步后 GREEN（2 files / 13 tests）。
- RED：V6 desktop/iPhone test initially failed，因为 strict response lineage 尚未收集；随后只修改 `apps/web/e2e/v0.6-learning-loop.spec.ts`，从实际 Core response schema 捕获 UUID `courseId → actionId → timeRequestId → scheduledEventId`，不使用 `.first()`、`.last()` 或 `.nth()`。
- P1 RED：Owner 已保存凭据时，`createLearningRun` 仍返回 `BLOCKED_PROVIDER`/`adapterKind=NONE`；缺失 Owner credential 也无法区分“生产 adapter 存在但需配置”。最小修复仅补 provider-neutral factory + metadata-aware disclosure，并在 claim transaction 后以 `withApiKey` 创建 DeepSeek text adapter。
- P1 GREEN：`learning.test.ts` 覆盖 configured→AWAITING_DISCLOSURE→一次 adapter、missing credential→BLOCKED/零 call、CredentialNotConfigured/SecretStore/adapter 失败的稳定 503、key 不入 request-visible payload/SQLite/response、解密/模型 observer 均为 `database.inTransaction=false`，以及 `LEARNING_PROPOSAL_CREATED.entity_type=LEARNING_RUN`。Web settings test 固定 disclosure 文案。
- P1 RED（capability evidence）：`npm test --workspace @ev/core -- tests/learning.test.ts tests/course-import.test.ts tests/v0.6-capability-gate.test.ts` 失败 4 项；production Search、Vision、Learning 的 preflight disclosure 都实际返回 `REAL_PROVIDER`（预期 `NONE`），共享 registry 亦把仅存在的 production adapter 标成 `REAL_PROVIDER`。
- P1 RED（Vision actual call）：`npm test --workspace @ev/core -- tests/course-import.test.ts` 在严格 Vision 输出失败路径得到 persisted `actual_calls=0`（预期 1）。
- P1 GREEN（capability evidence/call count）：共享 registry 对 production adapter 的 preflight 改为 `NONE`；`CapabilityRunRepository.complete` 显式持久化 terminal evidence；Vision/Search/Learning 仅在实际 adapter 成功后按 adapterKind 写入 `REAL_PROVIDER`/`AUTOMATED_FAKE`。所有失败写入 `NONE`。Learning 的局部 `providerCallStarted` 仅在进入 `adapter.generate` 前置位，CredentialNotConfigured、SecretStore unprotect、factory.create 失败均为 0，实际 generate 成功/失败为 1；Vision 同样区分 readFile 前置失败（0）与 strict Provider output failure（1），后者 RED 后 GREEN，前者以 runner-owned 临时 artifact 删除直接覆盖。测试均为注入 seam/临时目录，无真实 socket/DPAPI。
- Sol 审计证据未被改写：`.agent/reports/v0.6-v6-04-sol-upgrade.md`、`.agent/reports/v0.6-v6-04-sol-escalation-decision.md`。

## 冻结断言映射

| 冻结要求 | 实现与可观察验证 |
| --- | --- |
| Citation re-fetch/hash | Learning route/service 在 DeepSeek 文本建议前重新 metadata fetch、sha256 对比；hash 变化/获取失败 fail closed；`learning.test.ts` 覆盖。 |
| Provider 边界 | provider-neutral `LearningAdviceCapabilityFactory`；显式 TEST_FAKE override 优先且仍三重门。生产 default 在 Owner credential metadata=CONFIGURED 时 READY，claim 以后才通过 `withApiKey` 临时创建 DeepSeek text adapter；无配置 BLOCKED/明确提示，CredentialNotConfigured/SecretStore/adapter failure 稳定 503。无真实 socket 的 tests 覆盖。 |
| Capability evidence / calls | production adapter availability 本身只持久化/披露 `NONE`；production Vision/Search/Learning 成功才持久化 `REAL_PROVIDER`。TEST_FAKE 成功为 `AUTOMATED_FAKE`。失败不升级 evidence；Learning 对 credential/unprotect/factory 失败持久化 0 calls、generate 失败为 1，Vision strict output failure 为 1。`learning.test.ts`、`course-import.test.ts`、`v0.6-capability-gate.test.ts` 覆盖。 |
| Strict proposal | `CITED_LEARNING_ADVICE_V1` strict schema 只生成 LEARNING Proposal；接受前不写 Action/TimeRequest；contracts/core tests 覆盖。 |
| 原子 ACCEPT | 单 SQLite transaction 创建 course-linked Action、citation lineage、ACTIVE TimeRequest；REJECT/replay/concurrency/cross-Owner/failure 无部分事实；proposal API/repository tests 覆盖。 |
| 日计划与 Today | 既有 Daily Plan 消费 TimeRequest；Today 从 Action/TimeRequest 关系只读聚合 `courseId`，STUDY Event 带 action/course lineage；`today.test.ts` 与 Web Today tests 覆盖。 |
| Fake/E2E | 三重 gate 仍要求 test + explicit flag + runner-owned root；desktop+iPhone 真实路由 E2E 验证每门课程一条 Action/Event，desktop 后同名各 1，iPhone 后各 2，ID 集合严格等于两轮捕获结果。 |
| 版本与历史 | root/workspace package、lock metadata、`APP_VERSION`、health/run 与 CHANGELOG 均为 0.6.0；历史 migration app_version 断言仍通过。 |
| 迁移范围 | `git diff --exit-code -- apps/core/src/storage/migrations.ts` PASS；v19 未改变，未为 events 添加 `course_id`。 |

## 已执行验证

```powershell
npm test --workspace @ev/contracts -- tests/courses.test.ts tests/calendar-proposals.test.ts tests/providers.test.ts
# PASS: 3 files / 25 tests
npm test --workspace @ev/core -- tests/course-import.test.ts tests/calendar-repository.test.ts tests/learning.test.ts tests/public-resource-fetcher.test.ts tests/proposal-api.test.ts tests/today.test.ts tests/migrations.test.ts tests/v0.5-reliability-storage.test.ts tests/v0.6-capability-gate.test.ts
# PASS: 9 files / 43 tests
npm test --workspace @ev/web -- tests/core-client.test.ts tests/core-bff-idempotency.test.ts tests/schedule-workspace.test.tsx tests/learning-workspace.test.tsx tests/course-detail-workspace.test.tsx tests/provider-settings.test.tsx tests/today-dashboard.test.tsx
# PASS: 7 files / 47 tests
npm run typecheck
# PASS: 4 workspaces
npx eslint [V6-04 focused files and apps/web/e2e/v0.6-learning-loop.spec.ts]
# PASS
npm run build --workspace @ev/web
# PASS
npm run test:e2e -- e2e/v0.6-learning-loop.spec.ts
# PASS: 1/1 (desktop + iPhone)
git diff --check
git diff --exit-code -- apps/core/src/storage/migrations.ts
# PASS
```

本轮 P1 收口追加验证：

```powershell
npm test --workspace @ev/core -- tests/learning.test.ts tests/course-import.test.ts tests/v0.6-capability-gate.test.ts
# PASS: 3 files / 16 tests
npm test --workspace @ev/contracts -- tests/courses.test.ts tests/calendar-proposals.test.ts tests/providers.test.ts
# PASS: 3 files / 25 tests
npm run typecheck
# PASS: 4 workspaces
npx eslint apps/core/src/modules/providers/capabilities.ts apps/core/src/modules/providers/capability-run-repository.ts apps/core/src/modules/learning/service.ts apps/core/src/modules/calendar/import-service.ts apps/core/tests/learning.test.ts apps/core/tests/course-import.test.ts apps/core/tests/v0.6-capability-gate.test.ts
# PASS
git diff --check
git diff --exit-code -- apps/core/src/storage/migrations.ts
# PASS; migrations.ts/v19 zero diff
```

## 结论与证据债

P0=0，P1=0。未调用真实 Vision/Search/DeepSeek，真实 Provider smoke 为 `NOT RUN — APPROVAL REQUIRED`。未安装依赖、未读取/写入真实 `EV_DATA_DIR`、未使用真实凭据、未执行 Git staging/commit。`apps/core/src/storage/migrations.ts` 仍无 diff，v19 未改变。提交后仍需按冻结流程执行一次 v0.6 Sol 里程碑审查。
