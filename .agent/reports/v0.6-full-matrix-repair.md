# v0.6 full-matrix integration repair

Date: 2026-08-31
Agent: Terra
Git: not staged, not committed

## Main Agent RED

- Core full matrix: 45 files, 297 tests; 8 failed / 289 passed.
- Two recovery failures: startup `invalid daily plan preflight lifecycle state`; request path expected 503 but received 500.
- Six stale assertions: migration count/two frozen lists stopped at v18, and three current app-version fields expected 0.5.0.
- EPERM cleanup output was treated as a consequence of the failed assertion path; no cleanup implementation was changed.

## Root cause

- `providerReliabilityNow` was composed into the execution unit, idempotency service, and daily planning service, but not `createDailyPlanPreflightService`. The test preflight used the real 2026-08-31 clock while the fixed recovery sweep used 2026-08-24, so the unchanged v17 lifecycle trigger correctly rejected time reversal.
- Tests had not advanced their exact migration and app-version expectations to the existing v19 / `APP_VERSION=0.6.0` facts.

## Terra focused RED

`npm test --workspace @ev/core -- tests/v0.5-recovery-sweeper.test.ts` — exit 1, 2 failed / 3 passed with the same lifecycle and 500-vs-503 failures.

## Minimal repair

- `apps/core/src/app.ts`: conditionally pass `now: options.providerReliabilityNow` to `createDailyPlanPreflightService`; when the option is absent, production retains the service default real clock.
- `database.test.ts`: keep exact count and ordered migration assertions, advancing them to v19 `add_v06_learning_schedule_loop`.
- Daily planning service, Provider lease, and reliability repository tests now assert current persisted version via contracts `APP_VERSION`.
- No recovery algorithm, trigger, migration SQL, schema, Provider adapter, product scope, or Web source was changed.

## GREEN evidence

- Recovery focused: 1 file / 5 tests PASS.
- Five-file focused: 5 files / 24 tests PASS.
- Core full: 45 files / 297 tests PASS.
- Core typecheck: exit 0.
- Focused ESLint: exit 0.

## Root unit matrix RED before Web maintenance

`npm test` results:

- legacy: 5/5 PASS.
- Core: 297/297 PASS.
- Contracts: 77/77 PASS.
- Domain: 7/7 PASS.
- Web: 236/239 PASS, 3 failed.

Remaining Web failures are outside this repair's authorized files:

1. `daily-plan-workspace.test.tsx` could not locate the expected mobile CSS rule.
2. `v0.4-scheduling-workspaces.test.tsx` selected a 42rem Course detail media block that does not contain the expected legacy scheduling selectors.
3. `playwright-config.test.ts` timed out after 5 seconds while loading resolved configuration.

At that checkpoint Core integration was GREEN but the root command was not; the following Web maintenance section records the authorized follow-up and fresh full-root result.

## Boundaries

- E2E/build: not run, per instruction.
- Real Vision/Search/DeepSeek, credentials, DPAPI, and network: `NOT RUN — APPROVAL REQUIRED`.
- Migration SQL and immutable FAIL review: unchanged.
- Git add/commit/push: not run.

## Web matrix test-maintenance repair

### RED evidence

- Main Agent focused reproduction: `npm test --workspace @ev/web -- tests/daily-plan-workspace.test.tsx tests/v0.4-scheduling-workspaces.test.tsx tests/playwright-config.test.ts` — 2 failed / 61 passed; the Playwright config file passed 3/3 in isolation.
- Both CSS tests used `lastIndexOf('@media (max-width: 42rem) {')`, so the later v0.6 Course Detail media block hid still-present legacy responsive rules.
- The first minimal `indexOf` attempt proved insufficient: 1 failed / 62 passed because the existing regex matched a non-media 2.625rem rule between responsive blocks.

### Minimal repair

- The two CSS tests now collect every complete 42rem media block with balanced braces before applying their unchanged selectors and assertions.
- The three dynamic Playwright configuration tests each have a bounded `15_000ms` item timeout; assertions and production Playwright configuration are unchanged.
- No product CSS, component, migration, Provider, or production configuration was changed.

### GREEN evidence

- Three-file focused: 3 files / 63 tests PASS.
- Web full: 21 files / 239 tests PASS.
- Root `npm test`: legacy 5/5, Core 297/297, Web 239/239, Contracts 77/77, Domain 7/7 PASS; exit 0.
- Web typecheck: exit 0.
- Focused ESLint for the three Web tests: exit 0.
- `git diff --check`: exit 0.
- Working-tree migration SQL, immutable FAIL review, product Web CSS/components, and production Playwright config: zero diff.
- Git index: empty; no staging or commit attempted.
- E2E/build and real Provider/network execution were not run, per instruction.

## Tasks focus flaky test repair

### RED evidence and root cause

- Main Agent's full `npm test` session failed `tasks-workspace.test.tsx` at “edits a task with its current version and returns focus to the edit control”: the updated edit button was present, but `document.body` still had focus.
- Waiting only for the third fetch observed the reload request before the component's `settledReloadKey` focus effect had necessarily committed. The component's saved-focus implementation was unchanged.

### Minimal repair and GREEN evidence

- The final `toHaveFocus()` assertion remains exact but is now awaited with Testing Library `waitFor`; no timeout, selector, component, or production configuration changed.
- Exact `-t` run 1: 1/1 PASS, exit 0.
- Exact `-t` run 2: 1/1 PASS, exit 0.
- Exact `-t` run 3: 1/1 PASS, exit 0.
- `tasks-workspace.test.tsx`: 43/43 PASS.
- Web full: 239/239 PASS.
- Root `npm test`: legacy 5/5, Core 297/297, Web 239/239, Contracts 77/77, Domain 7/7 PASS; real session exit 0.
- Web typecheck and focused ESLint: exit 0.
- `git diff --check`: exit 0; Web product source, migration SQL, and immutable FAIL review remain zero-diff; Git index remains empty.

## Final pending-proposal locator repair

### RED and minimal repair

- RED was the preceding full E2E 7/8, where the page legitimately contained v0.4's completed proposal summary and v0.6's current pending proposal summary. The unscoped exact text locator therefore resolved to two elements even though only the current pending card exposed an exact `采用安排` button.
- `applyDailyPlan` now filters `article.daily-plan-review-card` by the descendant exact `采用安排` button, requires exactly one matching card, asserts the exact Fake summary inside that card, and obtains the exact apply button from the same card. No `.first()`, `.last()`, `.nth()`, page-wide count relaxation, or browser-error filtering was introduced.
- No product code, Fake provider, validator, time window, v0.4 flow, runner/config, migration, or immutable review changed in this repair.

### GREEN evidence

- Focused `npm run test:e2e -- e2e/v0.6-learning-loop.spec.ts`: 1/1 PASS, exit 0 (31.7s).
- Full `npm run test:e2e`: 8/8 PASS, real exit 0 (2.0m); v0.4 and v0.6 both passed in the shared managed run.
- `npm run typecheck --workspace @ev/web`: exit 0.
- Focused ESLint for `v0.4-ui-origin-scheduling.spec.ts` and `v0.6-learning-loop.spec.ts`: exit 0.
- `git diff --check`: exit 0.
- Working-tree migration SQL and immutable Sol FAIL review: zero diff.
- Real Vision/Search/DeepSeek and external network remain `NOT RUN — APPROVAL REQUIRED`; no staging or commit was performed.

## Main Agent independent final verification

- `npm test`: Legacy 5/5, Core 297/297, Web 239/239, Contracts 77/77, Domain 7/7; exit 0.
- `npm run typecheck`: all four workspaces passed; exit 0.
- `npm run lint`: exit 0 with zero errors and four existing warnings in unchanged `apps/core/tests/v0.5-recovery-sweeper.test.ts`.
- `npm run build`: Next.js production build passed and all routes were emitted; exit 0.
- `npm run test:e2e`: all eight specs passed in one shared runner-owned data directory; exit 0.
- `git diff --check`, explicit untracked report trailing-whitespace checks, migration zero-diff, immutable FAIL review zero-diff, five tracked package version checks, production-source credential-literal scan, and `next-env.d.ts` baseline check all passed.
- Real Vision/Search/DeepSeek, real credentials, DPAPI, and external network remain `NOT RUN — APPROVAL REQUIRED`.

## Trace-confirmed v0.6 learning-window repair and new full-suite stop

### RED, root cause, and minimal repair

- Prior full E2E RED was 7/8: the v0.6 desktop Daily Plan decision submitted an exact 12:00–13:00 APPLY window and received 422 because the shared managed run already contained v0.4's confirmed 12:00–13:00 Event. Core correctly rejected the overlap; browser evidence contained no console warnings or errors.
- Only `v0.6-learning-loop.spec.ts` learning inputs changed: desktop is now 16:00–17:00 and iPhone is 18:00–19:00. Both remain fixed 60-minute windows on Today, mutually non-overlapping and disjoint from v0.4's 12:00–13:00 Event.
- Real proposal decisions and the strict console, 401, UUID lineage, one-Action/one-Event, and overflow assertions remain unchanged. This step did not edit product code, Fake providers, validators, v0.4, runner/config, migrations, or the immutable Sol FAIL review.

### Focused GREEN and new full-suite stop

- Focused `npm run test:e2e -- e2e/v0.6-learning-loop.spec.ts`: 1/1 PASS, real exit 0 (31.2s).
- Full `npm run test:e2e`: 7/8 PASS, real exit 1; v0.4 passed in the shared managed run.
- New first failure: v0.6 reached `applyDailyPlan` line 184, where exact text `由测试 Fake Provider 生成的待审核安排。` resolved to two visible summary elements and Playwright rejected the strict locator before the next decision. Both attached browser evidence files reported no warnings or errors.
- Per the explicit stop condition, Terra did not change the locator, inspect a new trace, or run the post-GREEN Web typecheck, focused ESLint, diff-check, migration/review checks, or build.
- Real Vision/Search/DeepSeek and external network remain `NOT RUN — APPROVAL REQUIRED`.

## Full E2E minimal repair checkpoint

### Main Agent RED and minimal changes

- Main Agent full E2E RED: 5/8 PASS; owner keyboard order skipped the new default-unchecked “加入每日计划” checkbox, while v0.4 and v0.6 used credentials different from the existing shared runner Owner and received 401.
- Owner spec now asserts the real `priority → 加入每日计划 checkbox → 添加到今天` Tab sequence.
- v0.4 and v0.6 authenticate as `task16-e2e-owner` with the existing shared test password; all task/course/project/run suffixes remain unique.
- No product code, runner/config, browser-problem filter, console/network assertion, or single-Owner rule changed.

### Focused GREEN

- `owner-task-dashboard.spec.ts`: 1/1 PASS.
- `v0.4-ui-origin-scheduling.spec.ts`: 1/1 PASS.
- `v0.6-learning-loop.spec.ts`: 1/1 PASS.
- Each focused command used its own runner-owned data directory.

### Full-suite blocking evidence

- Full `npm run test:e2e`: 6/8 PASS, real exit 1.
- v0.4 progressed beyond authentication but failed at line 198: expected empty Fake evidence before its Provider call, but the shared full-suite runner directory already contained three Daily Plan Fake evidence entries from preceding specs.
- v0.6 progressed beyond authentication but failed its unchanged clean-browser assertion with `[console:error] Failed to load resource: the server responded with a status of 422 (Unprocessable Entity)`.
- The original keyboard failure and both 401 failures did not recur. Per instruction, Terra stopped after the full-suite failure and did not modify isolation, evidence expectations, problem filters, console/network assertions, product code, runner, or config.
- Web typecheck, focused ESLint, diff-check, and migration/review checks were not rerun after this failed full-suite gate.

## Trace-confirmed shared-state repair checkpoint

### RED and root cause

- Prior full E2E RED was 6/8: v0.4 compared shared managed-run Fake evidence with `[]`, and its excluded OPEN Task polluted the later v0.6 same-date schedule.
- Trace evidence identified the later v0.6 iPhone generate 422 as a real overlap with that excluded Task, not a validator or Fake defect.

### Minimal repair

- v0.4 captures the complete Fake evidence baseline before its Provider call, proves approval leaves that baseline byte-for-structure unchanged, and proves generation produces exactly `[...baseline, this test's unique invocation]`.
- Task creation responses are parsed with `taskResponseSchema`; v0.4 retains its excluded Task id/version and, in `finally` before evidence attachment, sends a real versioned `PATCH /api/core/tasks/:id` to `CANCELLED`, requiring status 200 and a strictly parsed updated Task.
- No evidence file was cleared, truncated, or deleted. No product, Fake, validator, runner/config, browser-problem filter, migration, or immutable review changed.

### Focused GREEN and new full-suite stop

- Focused v0.4: 1/1 PASS.
- Focused v0.6: 1/1 PASS.
- Full `npm run test:e2e`: v0.4 now passes in the shared managed run; total 7/8 PASS, real exit 1.
- New first failure: v0.6 desktop `applyDailyPlan` received 422 from the proposal decision POST at line 193, where 200 was required. Its attached desktop browser evidence reported no console warnings or errors.
- This is later and different from the previous iPhone generate 422. Per stop condition, Terra did not inspect the new trace, modify v0.6, or run Web typecheck, ESLint, diff-check, migration/review checks, or build after the failed full-suite gate.
- Real Vision/Search/DeepSeek and external network remain `NOT RUN — APPROVAL REQUIRED`.
- E2E/build and real Provider/network execution were not run, per instruction.

### Final same-root heading focus repair

- Main Agent's next full `npm test` session exposed the same post-save timing at “moves focus to the stable Tasks heading when a saved task disappears”: the empty page and heading were present while focus was still on `document.body`.
- Only that final heading `toHaveFocus()` assertion now uses default `waitFor`. The synchronous cancel-edit assertion remains unchanged; no timeout or component changed.
- Both post-save focus tests together, exact `-t` run 1: 2/2 PASS, exit 0.
- Both post-save focus tests together, exact `-t` run 2: 2/2 PASS, exit 0.
- Both post-save focus tests together, exact `-t` run 3: 2/2 PASS, exit 0.
- `tasks-workspace.test.tsx`: 43/43 PASS; Web full: 239/239 PASS.
- Root `npm test`: legacy 5/5, Core 297/297, Web 239/239, Contracts 77/77, Domain 7/7 PASS; real session exit 0.
- Web typecheck and focused ESLint: exit 0. No further same-root failure was observed.
- `git diff --check`: exit 0; Web product source, migration SQL, and immutable FAIL review remain zero-diff; Git index remains empty.
