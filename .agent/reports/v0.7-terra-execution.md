# v0.7 Terra execution handoff

Date: 2026-08-31 Asia/Shanghai  
Base: `785b7b6`  
HEAD: `76257a8`

## Atomic commits and task evidence

| Task | Commit | Exact message | RED (real first failure) | GREEN |
| --- | --- | --- | --- | --- |
| V7-01 | `0f0f64633a4962d8aa8ad26bb12dfab264776b23` | `feat(contracts): freeze v0.7 health loop` | Contracts focused command exit 1; new v0.7 schemas were absent (8 failed assertions). | Contracts focused 2 files / 13 tests PASS; typecheck, focused lint, diff check PASS. |
| V7-02 | `d524fc4ac901f9c8d5dc6b77e5b78fe46c5e3868` | `feat(core): add v0.7 health storage and policy` | Domain focused exit 1 (four missing exports); Core focused exit 1 (missing v20/repository modules). | Domain 4 and Core 20 focused tests PASS; affected typecheck, lint, diff check PASS. Migration v20 was added here only. |
| V7-03 | `25986b771aa1e0e0a71c1f6af623ad4799f69d8c` | `feat(fitness): add reviewed workout drafts` | Fitness route focused command exit 1: module absent. | 3 files / 7 tests PASS; typecheck, lint, diff check PASS. Acute pain blocks before catalog/provider. |
| V7-04 | `8f20e091f4d97e05641cafa6b3e304d054b7df5c` | `feat(fitness): confirm and complete workouts` | Endpoint focused command exit 1: 404. | 4 files / 16 tests PASS; typecheck, lint, diff check PASS. |
| V7-05 | `e8c6da6294d0568bf8f732f401b820ab8ca2edc2` | `feat(nutrition): confirm sourced decimal meals` | Nutrition focused command exit 1: expected v2 create 201, received 404. | 3 files / 7 tests PASS; typecheck, lint, diff check PASS. |
| V7-06 | `31692296ce157eb7b81170a0368f608a602c69dd` | `feat(app): wire gated v0.7 health capabilities` | Composition focused command exit 1: 404. | Core 6 files / 16 tests and runner 2 tests PASS; Core/Contracts typecheck, focused lint, diff check PASS. |
| V7-07 | `d71a2fed9deb558cd1747e54101d5b786f50fa74` | `feat(web): complete v0.7 health review loops` | Component focused command exit 1: missing `睡眠分钟` and `餐食文本` UI. Browser RED also exposed real 422 safety validation and an asynchronous source-save race. | Components 2 files / 6 tests PASS; Web typecheck, focused lint, diff check PASS; focused browser spec 1/1 PASS, exit 0. |

## Authorized mechanical version gate fix

Commit: `76257a8 test(v0.7): align legacy version gates`.

Only these two test expectations changed: `packages/contracts/tests/providers.test.ts:70` and `apps/core/tests/learning.test.ts:781`, each from `0.6.0` to the frozen `0.7.0` contract. No production code, other test, migration, fixture, or version metadata changed.

- RED: `npm test --workspace @ev/contracts -- tests/providers.test.ts` exit 1, 1 failed / 14 passed; `npm test --workspace @ev/core -- tests/learning.test.ts` exit 1, 1 failed / 11 passed. Each first failure was precisely the stale version assertion.
- GREEN: the same commands exited 0: Contracts 1 file / 15 tests passed; Core 1 file / 12 tests passed.

## V7-07 browser root-cause record

The initial desktop/iPhone test contexts collided on the same Owner/date daily-plan state. Creating a second Owner caused the observed real `401 Unauthorized`; that approach was reverted. The final test preserves one Owner and session and uses distinct Shanghai local dates. It waits for `POST /revisions` to return the expected revision schema after “保存来源选择”, asserts “确认这餐” is enabled, then clicks confirm. It does not filter console `401` or `422` errors. The actual focused command was:

```powershell
npm run test:e2e -- e2e/v0.7-health-loops.spec.ts
```

Initial focused result: 1 passed, exit 0 (27.7 s). The version-end repeat also passed: 1 passed, exit 0 (35.0 s). Browser problem arrays were empty.

## Migration, version, fixtures, and fail-closed evidence

- Migration `v20` was appended once in V7-02; V7-03..V7-07 had zero migration diff.
- V7-06 sets all package metadata and `APP_VERSION` to `0.7.0`.
- The internal starter fitness catalog is `FIRST_PARTY_INTERNAL`, `redistribution=false`, with manifest hash `cb280d075eb304b7c1127e412b70b9cfd90c62cdd410df2b622a846dfe3846dc`; no network, external source, license, or real API was used.
- Nutrition numeric values flow only through the provider interface. The local adapter is explicitly test-gated (`NODE_ENV=test` plus `EV_E2E_V07_HEALTH_TEST_ADAPTERS=1`) and labels evidence `TEST_FIXTURE`/`AUTOMATED_TEST_FIXTURE`. Production descriptors remain `NOT_CONFIGURED`/`NONE` with `NOT_RUN_APPROVAL_REQUIRED`; unavailable production provider returns 503.
- Decimal nutrient values remain exact string/BigInt-micros paths; no float conversion was introduced.

## Completed version-end matrix

The original post-V7 root run exposed only the two authorized stale assertions above. After commit `76257a8`, the first normal-sandbox `npm test` attempt returned exit 1 because 13 Core tests timed out and then received `EPERM` while cleaning their isolated `%TEMP%` directories; its Core result was 304 passed / 13 failed. Legacy (5/5), Web (246/246), Contracts (85/85), and Domain (11/11) passed in that attempt. This was an execution sandbox cleanup restriction, not a product assertion failure.

The exact same root command was then run once in the approved restricted environment and exited 0:

- `npm test`: Legacy 5/5, Core 50 files / 317 tests, Web 21 files / 246 tests, Contracts 7 files / 85 tests, Domain 4 files / 11 tests all PASS.
- `npm run build`: exit 0. The generated `apps/web/next-env.d.ts` build-only diff was restored to its committed dev references.
- `npm run test:e2e -- e2e/v0.7-health-loops.spec.ts`: 1 passed, exit 0 (35.0 s).
- `git diff --check 37982d8..HEAD`: exit 0, no output.
- `git diff --check 785b7b6..HEAD`: exit 0, no output. `785b7b6` is the original V7 execution baseline supplied at task start; it differs from the user-requested `37982d8`, so both were checked and reported.

## Changed areas

- Contracts: fitness/nutrition/capability/proposal schemas and public exports.
- Domain/Core: deterministic health policies, v20 storage, workout/meal proposal, confirm and completion paths, test-only adapters and fail-closed capability wiring.
- Web: fitness and nutrition workspaces/review components, responsive safe-area styling, component tests, and the v0.7 health-loop E2E.
- Version metadata: package versions and contract `APP_VERSION` are `0.7.0`.

No real provider, network, key, personal data, installation, deployment, push, tag, release, or Sol review was run. Those remain `NOT RUN — APPROVAL REQUIRED` where applicable.
