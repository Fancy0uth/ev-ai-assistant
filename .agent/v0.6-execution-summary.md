# v0.6 执行总结（Terra 工程交接）

日期：2026-08-31（Asia/Shanghai）

## 原子任务与提交

| 任务 | 状态 | 提交 |
| --- | --- | --- |
| V6-01 secure local timetable artifacts | 已提交 | `8ee52e1` |
| V6-02 review timetable candidates before scheduling | 已提交 | `4b4e218` |
| V6-03 save safe public course citations | 已提交 | `b99fdce` |
| V6-04 schedule cited study actions | 工程已完成；等待主 Agent 提交 | 未创建（Terra 未执行 git add/commit） |

> 最终收口：V6-04 已提交为 `a94e82e`。后续三轮独立 Sol 审查及定点修复最终在 TARGET `7ba44fb` 达到 P0=0/P1=0；PASS review 提交为 `b3b6409`。上表保留 Terra 初次交接时点，最终状态以本段和 `docs/reviews/2026-08-31-v0.6-sol-rereview-3.md` 为准。

## V6-04 验证结果

- Contracts focused：3 files / 25 tests PASS。
- Core focused：9 files / 43 tests PASS。
- Web focused：7 files / 47 tests PASS。
- root `npm run typecheck`：4 workspaces PASS；focused ESLint PASS；`npm run build --workspace @ev/web` PASS。
- `npm run test:e2e -- e2e/v0.6-learning-loop.spec.ts`：1/1 PASS，覆盖同一 Owner 的 desktop+iPhone 真实路由闭环。E2E 从 Core response 收集 `courseId → actionId → timeRequestId → scheduledEventId` UUID，desktop 后同名 Action/STUDY Event 各 1，iPhone 后各 2，ID 集合精确匹配两轮捕获值。
- `git diff --check` PASS；`git diff --exit-code -- apps/core/src/storage/migrations.ts` PASS。V6-04 没有修改 migrations.ts 或 v19。
- 主 Agent 复核指出的 Owner credential 编排 P1 已关闭：生产默认在 metadata=CONFIGURED 后给出 READY disclosure，claim 提交后才 `withApiKey` 临时构造文本 adapter；missing credential 零 adapter call，SecretStore/model/CredentialNotConfigured 安全 503，key 不入 payload/SQLite/response，解密与模型调用可观察为 transaction 外。设置页文案已同步。
- 最后 capability evidence/call-count P1 已关闭：production adapter 的 preflight disclosure/run 均为 `NONE`，仅实际成功的 Vision/Search/Learning terminal run 按实际 adapterKind 写 `REAL_PROVIDER`，成功 TEST_FAKE 为 `AUTOMATED_FAKE`；失败不会升级证据。Learning 的 CredentialNotConfigured、SecretStore、factory.create 失败均 `actual_calls=0`，进入 generate 后失败为 1；Vision runner-owned artifact read failure 为 0、strict output failure 为 1。RED 分别观察到错误的 preflight `REAL_PROVIDER`（4 项）和 Vision `actual_calls=0`（预期 1）；GREEN 复跑 Core 3 files/16、Contracts 3 files/25、root typecheck、focused ESLint 均 PASS。

## 风险、债务与主 Agent 复核

- P0=0，P1=0。
- 真实 Vision/Search/DeepSeek smoke：`NOT RUN — APPROVAL REQUIRED`；生产未配置时保持诚实 `BLOCKED_PROVIDER`。没有真实 Provider、socket、凭据、依赖安装或真实用户数据访问。
- 保留 Sol 审计文件 `.agent/reports/v0.6-v6-04-sol-upgrade.md` 和 `.agent/reports/v0.6-v6-04-sol-escalation-decision.md`；已按 RETURN TO TERRA 仅在 E2E spec 完成身份修复。
- Terra 初次交接时要求主 Agent 审阅并提交 V6-04、再发起 v0.6 Sol 审查；该后续流程现已完成。真实 Provider evidence 仍不在未授权范围内。

## 最终里程碑结论

- 最终实现 TARGET：`7ba44fb8beec01545c64380838ed04341df99c1d`。
- 最终准入文档提交：`b3b6409 docs(review): pass v0.6 release readiness`。
- Sol rereview-3：PASS；P0=0、P1=0、P2=6、P3=0。
- root tests 634/634、typecheck/build PASS、lint 0 error/4 个既有 warning、desktop+iPhone E2E 8/8。
- 真实 Provider 与个人数据路径继续为 `NOT RUN — APPROVAL REQUIRED`，不影响本地确定性工程 PASS。
