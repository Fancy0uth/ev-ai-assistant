# C1 R3 限定收尾证据

## 引擎恢复后的最终运行证据

下方历史 NOT RUN/BLOCKED 保留，现由本节补齐 C1 所列运行检查；不覆盖未验证的真实认证/exec/Provider。

- `docker --context desktop-linux build --pull=false --network=default --label ev.component=codex --tag ev-codex-preflight:0.146.0-20260916-r3 scripts/codex-container` exit0。下载137138170字节、96.45s、SHA512 PASS，99.51s输出codex-cli0.146.0，无原home警告。镜像ID `sha256:fbd9b8228cc574f69550815ceb3de4ec3a33b144d659d7c297167e05436adeee`。
- 使用下列参数运行同一容器，Python先断言 UID=65532 与 HOME/CODEX_HOME=/home/ev-codex，再以20s期限执行 `codex --version`，断言 exit0、stdout=codex-cli0.146.0、stderr为空，随后以20s期限执行镜像内 isolation-probe.py。总exit0，七项均PASS：cap_eff_zero、egress_blocked、host_paths_absent、no_new_privs、non_root、rootfs_write_rejected、tmp_roundtrip。

```text
docker --context desktop-linux run --name ev-codex-c1-20260916-r3 --pull=never
  --label ev.component=codex --user 65532:65532 --network none --read-only
  --cap-drop ALL --security-opt no-new-privileges --pids-limit 64 --memory 256m --cpus 1
  --tmpfs /tmp:rw,nosuid,nodev,noexec,size=64m,mode=1777
  --tmpfs /home/ev-codex:rw,nosuid,nodev,noexec,size=64m,uid=65532,gid=65532,mode=0700
  --entrypoint python ev-codex-preflight:0.146.0-20260916-r3 -c <上述版本断言及probe顺序调用>
```

- 同一容器 inspect exit0：User65532:65532，Image为上列ID，Mounts=[]、Binds=null、NetworkMode=none、ReadonlyRootfs=true、CapDrop=[ALL]、CapAdd=null、Privileged=false、SecurityOpt=[no-new-privileges]，两个Tmpfs与上列一致；Memory268435456、PidsLimit64、NanoCpus1000000000、PortBindings={}、ExitCode0。容器已停止，保留供追溯；未挂宿主目录/认证、未发布端口。
- 再次引擎版本查询exit0、29.6.2；`python -B -m unittest discover -s scripts/codex-container -p test_fetch_codex.py -v` 本轮再次运行exit0、1/1 PASS，0.073s。三文件SHA256与下面既有静态复核记录一致，没有再次改业务代码。
- 结论：C1限定通过。没有登录，没有真实调用（0/1），没有全量应用测试、完整 helper/exec 或受控上游网关验证。无网络probe不能替代联网运行的边界证明；Docker当前恢复不代表Windows端点故障永久解决。

## 后续 Docker 定点恢复（用户另授权）

- 用户随后“允许”定点修复 Docker。只读确认 `run` 普通目录含三个零字节运行端点；Windows 对 dockerInference 报 1920，Docker 已退出。将明确路径 `C:/Users/asus/AppData/Local/Docker/run` 改名保留至同级 `run.ev-recovery-20260916`，新建 run 后启动已安装 Docker Desktop。exit0，不删除端点。
- 新日志不再卡 inference，改为 Secrets Engine `C:/Users/asus/AppData/Local/docker-secrets-engine/engine.sock` 同类错误。目录只含这一零字节端点，非普通秘密文件。正常 `docker desktop stop --timeout 20` 未成功；已崩溃状态下 `docker desktop stop --force --timeout 20` exit0。确认 Docker 进程已退出，目录非 reparse，目标备份不存在，将目录改名为 `docker-secrets-engine.ev-recovery-20260916` 保留后重新启动，exit0。
- 两次均在单一 PowerShell 内以 LiteralPath 操作已核对的明确绝对路径。没有 reset/prune、镜像/容器/卷/虚拟磁盘删除、设置修改、安装或主机重启。备份端点目录保留，不建议在引擎运行时换回。
- 同类一手问题报告：[Docker desktop-feedback #554](https://github.com/docker/desktop-feedback/issues/554)。其恢复方式只是定位参考；本机成功与否以实际引擎响应为准，不能据此认定底层 Windows 问题永久修复。
- 第2次启动仍 FAIL：第1次推进至 Secrets Engine 之前新建的 inference socket 在崩溃后再次残留。确认新的 run 仅含创建于本轮 17:17:30 的 dockerInference、secrets 原路径尚未重建；停止已崩溃 Docker 后，将这一新 run 保留为 `run.ev-recovery-20260916-attempt2`，新建空 run，确保两类旧端点同时不在原路径，再进行第3次启动（最后一次本轮恢复尝试）。仍没有删除文件或扩大到系统设置。
- 第3次启动后实际 `docker --context desktop-linux version --format '{{.Server.Version}}'` exit0，返回 `29.6.2`。引擎恢复可用；这是当前恢复证据，不保证重启后不复发。开始原授权的 R3 镜像重建，tag 为 `ev-codex-preflight:0.146.0-20260916-r3`，只使用 `scripts/codex-container` 的 9.38kB 上下文，旧镜像/容器保留。

用户在原两轮修复后另授权一次收尾。沿用 `codex/product-prd`，HEAD `50b533804be24f8963492679127f72e39ff4f7dd`，既有未提交修改全部保留。本文件只存本轮证据；进度唯一入口是 `plans/TASKS.md`。

## 任务契约与修复前状态

- 包成员必须精确等于 `package/vendor/x86_64-unknown-linux-musl/bin/codex`。原代码 `member.name.endswith(CODEX_SUFFIX)` 接受额外前缀，不满足契约。
- 将容器 HOME/CODEX_HOME 从 `/tmp/ev-codex` 移至 `/home/ev-codex`，与 mkdir/chown 一致。运行时使用专属、有界、UID/GID 65532、0700 的 tmpfs；不挂载宿主 HOME 或凭据。
- 保留固定下载地址、SHA512、大小/期限限制、普通文件校验、非 root、只读根、禁提权、cap-drop、无网络预检。
- 一个离线合成回归覆盖精确成员成功和前缀成员拒绝；主执行新镜像构建、版本输出无原警告、七项隔离检查，并 inspect 同一容器。
- 本轮不测试登录/推理，不增补完整 exec 资源，不扩展业务。真实调用 0/1，不提交/推送。

## 实际环境事件

开始时 Docker local named pipe 不存在。常规 Program Files 路径不存在，启动命令失败且无状态修改。通过已安装程序登记定位到 `C:/Users/asus/AppData/Local/Programs/DockerDesktop/Docker Desktop.exe`，以 Hidden 启动已安装程序，命令 exit0；引擎就绪仍需单独确认。未安装或改变全局配置。

## 检查结果

Terra 报告 RED/GREEN：以 `C:/Users/asus/AppData/Local/Programs/Python/Python311/python.exe` 运行 `scripts/codex-container/test_fetch_codex.py`。修复前 exit1，`AssertionError: FetchError not raised`、1 个失败；修复后 exit0，`Ran 1 test / OK`。主下面另有最终代码实际运行结果。

Docker 运行检查 BLOCKED：`docker --context desktop-linux version --format '{{.Server.Version}}'` 初次 exit1（named pipe 不存在）；启动后的查询无响应，由主终止该查询，exit1。读取 Docker backend 日志最后 22 行定位：2026-09-16T09:00:52Z backend crashed，`initializing Inference manager` 无法移除 `C:/Users/asus/AppData/Local/Docker/run/dockerInference`，同时报告 filename/directory/volume syntax incorrect。不是 EV 镜像构建错误。

未重置 Docker，未清理该端点，未终止其他项目容器或改变 Docker 设置。R3 镜像重建、非 root 版本警告复验、七项 probe 与 inspect 本轮 NOT RUN；旧镜像通过记录不能代替新代码验证。离线回归与限定静态复核结果待补。

主实际运行 `python -B -m unittest discover -s scripts/codex-container -p test_fetch_codex.py -v`：exit0，1/1 PASS（0.067s），正确成员提取字节一致，额外前缀抛出 `tar/TAR_TARGET_COUNT`。仅合成临时文件，不发起网络下载。新增测试完整正文见 `scripts/codex-container/test_fetch_codex.py`；两份生产文件的 R2→R3 修复差异见同目录 `2026-09-16-codex-c1-r3.diff`。这不是完整 exec 或认证验证。

独立 Astra medium Goodall `01a0a976-52f2-7850-83a3-801f24f076f7` 只读复核：两个代码级问题 ADDRESSED，规格/质量限定通过，无新增 actionable finding；未重复运行测试。运行时验证仍 BLOCKED/NOT RUN，不是完整 C1 PASS。

主核对 SHA256：Dockerfile `36E97D37B20384A51E7A0834300D5AD7DA13DDC907624C6C6E24D0E03518650E`；fetch-codex.py `BF46163C51C27EE03485E4C8CC84A44991C53F231D2278D8D4BED13518E991F5`；test_fetch_codex.py `4FBC6AC117B958D38F9414A6036748E01132F81378A1F42A8C066FE3603EBAB6`。限定 `git diff --check` exit0，但这些文件未跟踪，该命令不构成其完整空白检查或运行验证。
