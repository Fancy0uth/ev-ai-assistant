# FIT-01 实施证据（2026-09-12）

## 范围

- 新增纯函数 `apps/core/src/modules/fitness/external-catalog.ts`，不接生产入口、不读文件/网络/SQL、不导入真实数据或媒体。
- 新增单一组合用例 `apps/core/tests/external-exercise-catalog.test.ts`，只使用两条合成记录。
- 输出固定来源 `hasaneyldrm/exercises-dataset`、MIT、传入的 40 位 revision 和文字字段；媒体及未知原始字段不返回，安全状态固定为 `UNREVIEWED`。
- 本报告不更新 TASKS，也不构成完整动作库导入、医学审核或健身 UI 完成声明；进度与独立复核由 controller 处理。

## RED / GREEN

命令始终为：

```text
npm test --workspace @ev/core -- tests/external-exercise-catalog.test.ts
```

1. 初始 RED：退出码 1；命中 1 个测试、1 个失败。实际 `toEqual` 断言在成功解析路径失败：期望两个合成文字动作及 revision，导出占位实际返回空 `items` 和空 revision。这是断言 RED，不是 import 错误。
2. 初始 GREEN：退出码 0；1 个测试文件、1 个测试通过；输出无警告。
3. 上游源模式澄清后的校正 RED：合成输入改为实际 raw 字段 `body_part`、`secondary_muscles`，并含须忽略的 `muscle_group`、`category`。退出码 1；命中 1 个测试、1 个失败，旧 camelCase 输入读取导致受控 `EXTERNAL_EXERCISE_CATALOG_INVALID`。
4. 最终 GREEN：在纯解析器中将 raw snake_case 映射到 camelCase 输出后，退出码 0；1 个测试文件、1 个测试通过；输出无警告。

组合用例覆盖两条合成记录的中英文 steps、缺 steps 的逐语言文字回退、固定来源、`UNREVIEWED`、媒体/未知字段省略、输入未变，以及重复 ID 与显式损坏步骤的受控整批拒绝。

## 局部修复计数

- 初始实现后局部修复：1 / 2。
- 唯一修复是根据后续上游 raw schema 澄清，将输入读取从错误的 camelCase `bodyPart` / `secondaryMuscles` 更正为 `body_part` / `secondary_muscles`；输出契约保持 camelCase。

## 最终 SHA-256

- `apps/core/src/modules/fitness/external-catalog.ts`: `C6B5C8B261C4D39A1E1F7CC086E974AD8CEFD0EF99DB113C4BAD4BAD20FBB751`
- `apps/core/tests/external-exercise-catalog.test.ts`: `960E25E17842255AC09BB72889DDB93C757CD456A47C9AF33064B4F9B481C2D5`

未运行根测试、整包测试、typecheck、build、浏览器、网络、安装、真实数据或真实模型。
