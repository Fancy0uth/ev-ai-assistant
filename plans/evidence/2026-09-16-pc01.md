# PC-01 evidence — 2026-09-16

## Scope and safety

- Worktree: `C:/Users/asus/Documents/ev-ai assistant/.worktrees/product-prd`
- Branch / starting HEAD: `codex/product-prd` / `50b533804be24f8963492679127f72e39ff4f7dd`
- Implementation scope: only `apps/core/src/storage/migrations.ts` (append migration 27), `apps/core/src/modules/projects/conversation-repository.ts`, and `apps/core/tests/project-conversation-repository.test.ts`.
- The pre-existing dirty working tree was retained. No PC-02 files were read or changed.
- All fixture data was synthetic and stored in one `:memory:` SQLite database. No model, Provider, network, install, service startup, real data, stage, commit, stash, or push was used.

## Contract implemented

- Migration 27, `add_project_conversation_storage`, adds owner/project-bound conversations and USER-only messages without changing migrations 1–26 or generic agent tables.
- The composite foreign key to `project_scopes(owner_id, id)`, owner/project uniqueness, immutable conversation identity, message session foreign key, unique `(session_id, client_message_id)`, unique message ID, and `INTEGER PRIMARY KEY AUTOINCREMENT` sequence enforce the storage boundary.
- The repository validates Owner/project scope for every operation; it returns `404 PROJECT_SCOPE_NOT_FOUND` for missing or non-owner scope access, `400 PROJECT_MESSAGE_INVALID` for invalid message/list input, and `409 PROJECT_MESSAGE_CONFLICT` for same client message ID with different content.
- Reads do not create conversations. Appends trim and persist a USER message before any future Provider path, use a transaction, replay identical client IDs without a duplicate, and never write Task, Action, TimeRequest, Proposal, or Event rows.

## One synthetic in-memory case

`project-conversation-repository.test.ts` is one case. It starts at migration 26, inserts a synthetic Owner, two project scopes, one pre-existing Task, and generic agent-session/message rows, then upgrades only to 27. It verifies preservation, one conversation per Owner/project, ordered cursor reads, replay/conflict semantics, project isolation, controlled invalid-input failures, Owner/scope 404 behavior with unchanged counts, and zero unrelated business writes.

## TDD and verification

| Phase | Command | Exit / result |
| --- | --- | --- |
| RED | `npm test --workspace @ev/core -- tests/project-conversation-repository.test.ts` | exit 1; 1 file / 1 case failed at the intended v27 migration assertion: received `undefined`, expected `{ version: 27, name: 'add_project_conversation_storage' }`. The repository import was not reached. |
| GREEN | `npm test --workspace @ev/core -- tests/project-conversation-repository.test.ts` | exit 0; 1 file / 1 case passed. |
| Final targeted verification | `npm test --workspace @ev/core -- tests/project-conversation-repository.test.ts` | exit 0; 1 file / 1 case passed. |
| Scoped hygiene | `git diff --check -- apps/core/src/storage/migrations.ts` plus trailing-whitespace scan of the three implementation files | exit 0; `TRAILING_WHITESPACE=0`. |

- Initial implementation passes: 1.
- Local repair passes: 0 of 2 allowed.
- Test cases: 1 total, 1 passed, 0 failed, 0 skipped.

## SHA-256 (final implementation files)

| File | Lines | SHA-256 |
| --- | ---: | --- |
| `apps/core/src/storage/migrations.ts` | 2292 | `8B70CD9BDC0E803A7776A71A1C6AF9B43009EBC1070839EE1783561D44B33C82` |
| `apps/core/src/modules/projects/conversation-repository.ts` | 184 | `B49C06943C1E573A04AF2A643E77892471761F8C3F9AA9A6ED2E9B60C961347F` |
| `apps/core/tests/project-conversation-repository.test.ts` | 223 | `9417A1D2EA8A1439FB088D40F6405F70B32E0752196E96609C0F09CC470A4EE3` |

## Warnings and deliberately unrun checks

- Git reported its existing Windows line-ending notice for the already-dirty `migrations.ts`: LF will be replaced by CRLF when Git next touches it. The scoped diff check still exited 0.
- The RED command emitted npm's available-major-version notice; no package was installed or changed.
- Per the controller constraint, no full suite, typecheck, build, browser run, migration matrix, Provider/UI work, or real-data verification was run. Those are outside PC-01 evidence, not a claim that they pass.

## R1 — accepted compiler and UUID validation repair

- Scope remained PC-01 only: `conversation-repository.ts`, its existing one-case test, and this evidence report. Migration 27 was not changed; its SHA-256 remains `8B70CD9BDC0E803A7776A71A1C6AF9B43009EBC1070839EE1783561D44B33C82`.
- Root cause 1: values destructured from an `unknown` cursor input stayed `unknown`; `Number.isInteger` and `Number.isSafeInteger` do not provide the required TypeScript narrowing. The pre-repair Core typecheck reported six diagnostics at repository lines 104–106.
- Root cause 2: the handwritten UUID regex accepted only versions 1–5. Installed Zod `4.4.3`, already imported elsewhere in Core as `* as z`, accepts UUIDv7 and rejects the malformed test ID without an install.
- The same synthetic in-memory case now appends the valid UUIDv7 `01890f4e-2b49-7c0d-8a2a-0a4ec1f8e9d1`, retains the malformed-ID `400` assertion, and adds unknown `limit` and `beforeSequence` `400` assertions. It remains one test case.

| Phase | Command | Exit / result |
| --- | --- | --- |
| Compiler reproduction | `npm run typecheck --workspace @ev/core` | npm exit 1 / inner `tsc` exit 2; six diagnostics, all from un-narrowed `limit` / `beforeSequence` at lines 104–106. |
| UUIDv7 RED | `npm test --workspace @ev/core -- tests/project-conversation-repository.test.ts` | exit 1; 1 file / 1 case failed at the valid UUIDv7 append with `400 PROJECT_MESSAGE_INVALID` from `normalizeMessageInput`. |
| GREEN case | `npm test --workspace @ev/core -- tests/project-conversation-repository.test.ts` | exit 0; 1 file / 1 case passed. |
| GREEN Core check | `npm run typecheck --workspace @ev/core` | exit 0; `tsc --noEmit -p tsconfig.json` completed without diagnostics. |

- R1 combined 2 accepted findings in 1 local repair pass; repair count is now 1 of 2 allowed. No R2 was used.
- R1 test counts: 1 file, 1 case, 1 passed, 0 failed, 0 skipped.
- No warnings were emitted by the GREEN test or GREEN Core typecheck. No migration, package, installation, network, model, Provider/UI, PC-02, real-data, staging, commit, stash, or push operation occurred.

| File | Lines | SHA-256 after R1 |
| --- | ---: | --- |
| `apps/core/src/modules/projects/conversation-repository.ts` | 191 | `93474045B4A9C938FD9CDB98A3BADBBD7818D5667620E399CB093904FFCD187A` |
| `apps/core/tests/project-conversation-repository.test.ts` | 230 | `4809B7CA7D5C4785D69C0B57E99FD72B347BFDD6EF9CDCF59B897A9B4D108DC7` |
