# v0.7 Sol adversarial whole-version review

## Verdict

**FAIL** — P0: **0**, P1: **9**, P2: **3**, P3: **0**.

The aggregate unit, typecheck, build, lint, and focused browser commands are green, but they do not exercise or disprove the reproduced P1 contract failures below. Under the frozen rule that PASS requires P0=0 and P1=0, this target cannot pass.

## Review target and authority

- User-lineage base: `37982d8ddaee9eeaf1c0c7b0dc729ad03e7b2285`
- Execution base: `785b7b64c0cf0abf0a0c9274c99c7038e5d2f2b1`
- Target / inspected HEAD: `7db852f5a241409f90975fc54bdf2923fa66a196`
- Primary artifact range: `785b7b6..7db852f`
- Lineage range also inspected: `37982d8..7db852f`
- Frozen design: `docs/superpowers/specs/2026-08-31-v0.7-fitness-nutrition-execution-design.md`
- Frozen plan: `docs/superpowers/plans/2026-08-31-v0.7-fitness-nutrition-loop.md`
- Execution handoff reviewed: `.agent/reports/v0.7-terra-execution.md`
- Scope: 63 changed files / 6,108 insertions / 344 deletions in the execution range; 67 files / 7,918 insertions / 358 deletions in the lineage range.

## Findings

| Stable ID | Severity | Path + line | Reproduction / evidence | Expected | Actual | Root cause | Required action |
| --- | --- | --- | --- | --- | --- | --- | --- |
| V07-AUTH-001 | P1 | `apps/core/src/modules/health-loop/routes.ts:13-20`; `apps/core/src/app.ts:289-290`; `apps/core/tests/v0.7-composition.test.ts:5-9` | Build the default app and inject unauthenticated `GET /v1/health-capabilities`; the probe returned `200` and all three descriptors. The focused composition test also explicitly expects unauthenticated `200`. | Every new route requires the existing Owner session; unauthenticated access returns the existing `401` shape. | The route has no auth guard and discloses configured provider IDs/labels and evidence state before login. | `registerHealthLoopRoutes` was designed without `AuthService`, and its test froze the opposite of the Owner-session contract. | Add the existing auth guard, pass `AuthService` into registration, change the regression test to require `401`, and add an authenticated `200` case. |
| V07-IDEM-002 | P1 | `apps/core/src/modules/health-loop/idempotency-service.ts:133-159`; `apps/core/src/modules/fitness/routes.ts:43-104`; `apps/core/src/modules/nutrition/routes.ts:46-84`; `apps/core/src/http/api-error.ts:42-52` | Two authenticated check-in POSTs reused one key with a changed `energyLevel`; first returned `201`, second returned `500 INTERNAL_ERROR`. A separately seeded live local claim returned `409 IN_PROGRESS` with `Retry-After: null`. | Different semantic hash returns `409 IDEMPOTENCY_KEY_REUSED`; live work returns `409` with `Retry-After: 1`; terminal replay preserves exact status/body. | `V07IdempotencyConflictError` escapes all v0.7 routes into the generic 500 handler, and service-thrown `IN_PROGRESS` has no route/header mapping. | v0.7 routes bypass the established claim-to-HTTP adapter used by Proposal/Daily Plan routes. | Centralize v0.7 idempotency error mapping, catch collision explicitly, emit the frozen error code and `Retry-After: 1`, and add route-level collision/live-claim tests for every command family. |
| V07-IDEM-003 | P1 | `apps/core/src/modules/fitness/service.ts:191-272`; `apps/core/src/modules/nutrition/service.ts:161-172,192-217,256-291`; `apps/core/src/modules/health-loop/repository.ts:192-220`; `apps/core/src/app.ts:133-157` | Direct service probe called the same assisted-workout command twice: first succeeded with `replayed:false`; second threw `SQLITE_CONSTRAINT_UNIQUE` and left an extra `AWAITING_DISCLOSURE` row. A second probe seeded a `RUNNING` nutrition run whose lease expired in 2020; replay still returned `409 IN_PROGRESS` and left it `RUNNING`. | One provider call per key, exact stable replay, semantic collision detection, and expired/crashed claims terminalized before retry; no partial capability/domain state. | Assisted fitness never uses `v07_idempotency_records`, creates a new run before checking the key, hashes only goal/citations, completes the run before non-atomic workout/audit writes, and cannot replay. Nutrition treats every `RUNNING` row as live forever and converts terminal failures to a generic 503 instead of replaying the original response. Startup sweeps only the older capability system. | The external lifecycle is split across two unrelated tables without a recovery/sweeper transaction or a single replay state machine. | Implement one crash-safe external-command state machine for all three capabilities: lookup/hash check before insert, lease expiry terminalization, exact success/failure snapshots, atomic local finalization, and startup/next-request recovery. Add crash-point, same-key, different-hash, failed-response, and stale-lease tests. |
| V07-PROVIDER-004 | P1 | `apps/core/src/modules/fitness/service.ts:110-129`; `apps/core/src/modules/nutrition/provider.ts:51-65,69-104`; `apps/core/src/modules/nutrition/service.ts:201-215,269-290`; `apps/core/src/storage/migrations.ts:1461-1462` | A provider that ignores `AbortSignal` left both workout and meal operations unsettled after 8,250 ms. A strict-schema parser result of 30 bounded candidates serialized to 12,331 UTF-8 bytes and was accepted by `executeMealCandidateParse`. | Hard eight-second deadline and pre/post validation of complete serialized input ≤24,000 UTF-8 bytes and output ≤12,000 bytes; breach returns controlled `503` with no domain write and a terminal run. | Nutrition creates never-aborted signals; fitness only signals abort and awaits forever if the adapter ignores it. Neither provider boundary enforces serialized byte limits. Oversize nutrition output reaches a SQL CHECK during finalization, which can surface 500 and leave the external run stuck. | Abort signaling was mistaken for a deadline, and byte limits were recorded after calls instead of enforced at the port boundary. | Use a rejecting deadline race plus abort, compute complete UTF-8 payload sizes before/after the call, reject oversize output before domain work, terminalize runs reliably, and test signal-ignoring providers and multibyte limits. |
| V07-DECIMAL-005 | P1 | `packages/contracts/src/nutrition.ts:4-6,54-68`; `packages/domain/src/nutrition.ts:12-35,37-53`; `apps/core/src/modules/nutrition/repository.ts:375-395`; `apps/core/src/modules/nutrition/service.ts:293-308` | Probe results: positive schema rejected `"0"` but accepted `"0.0"` and `"0.000000"`; canonical schema accepted trailing forms `"1.0"` and `"1.2300"`. `scaleNutrientV1('999999.999999','999999.999999','0.000001')` returned `999999999998000000.000001`, outside the six-integer-digit contract. | Reject non-canonical trailing forms and every mathematical zero where positive is required; detect intermediate/result overflow and return `422 DECIMAL_OUT_OF_RANGE` without a Meal. | Zero denominators can enter persisted source snapshots and fail later as an uncaught `RangeError`; valid maximum inputs can generate non-canonical oversized strings and then fail as raw SQLite/500. No catch maps either path to the frozen 422 code. | Positivity compares spelling instead of numeric micros, and formatter/scale/totals have no upper-bound guard or domain error translation. | Define one canonical decimal grammar (including trailing-zero rules), test numeric positivity after parsing, bound every formatted entry/total, map range failures to `DECIMAL_OUT_OF_RANGE`, and add zero-spelling/max-product/max-total route tests. |
| V07-MIG-006 | P1 | `apps/core/src/storage/migrations.ts:1192-1415` | With the repository's established two-owner fixture technique (`ignore_check_constraints` only for inserting the second Owner), SQLite accepted an `owner-a` `fitness_check_ins_v2` row whose `signal_id` belongs to `owner-b`. The same single-column FK pattern is repeated for workouts, proposals, actions, time requests, revisions, source snapshots, meals, and entries. | Frozen v20 requires Owner-correlated foreign keys wherever SQLite permits composite uniqueness, with service checks as defense in depth. | v20 references only globally keyed IDs and creates no composite parent uniqueness/FKs, so the database accepts cross-Owner lineage. | Owner isolation was implemented only in service queries, contrary to the explicit migration invariant. | Add permitted composite unique indexes and `(owner_id, foreign_id)` FKs to all new lineage edges (within an additive corrective migration), then add negative cross-Owner insertion tests for each aggregate. |
| V07-MIG-007 | P1 | `apps/core/tests/migrations.test.ts:1221-1242`; `apps/core/tests/database.test.ts` | The only v20 upgrade test starts at v19, inserts one Owner, checks that Owner/rootpage plus two table names, and runs migrations twice. There are no v1, v2, v16, v17, or v18 fixtures and no byte-equivalent snapshots of Task, Agent run, Proposal, Signal, legacy meal, or historical app-version rows. | The frozen acceptance requires v1/v2/v16/v17/v18/v19 upgrades through normal startup, two runs, and byte-equivalent IDs/values/counts for all named legacy aggregates. Missing critical regression proof is P1. | Root tests pass without executing the mandated migration compatibility matrix. | The migration test was reduced to a narrow v19 smoke test and the handoff overstates coverage. | Add the exact six-version fixture matrix and full snapshots, including historical `0.5.0`/`0.6.0` rows and normal startup twice. Do not release based on the current smoke test. |
| V07-FIXTURE-008 | P1 | `apps/core/src/app.ts:107-120`; `apps/core/e2e/daily-plan-test-bootstrap.ts:29-40,224-236`; `tests/e2e-runner.test.mjs:29-47` | On Windows, `relative('C:\\runner-owned','D:\\outside\\app.sqlite')` returns the absolute `D:\\outside\\app.sqlite`; the implementation's `!relative(...).startsWith('..')` evaluates true. `artifactRoot` is never checked. Static tests merely regex-match flag names and do not exercise any negative gate. | `TEST_FIXTURE` must be rejected unless runtime test mode, explicit runner flag, and every resolved DB/data/artifact path is inside the runner-owned root; the frozen plan says a fixture outside its gates is an automatic FAIL. | A cross-volume database path passes the gate, and an arbitrary external artifact path is accepted. | Path containment omits `isAbsolute(relativePath)`/boundary validation and validates only `databasePath`. | Use a shared real-path-aware containment function that rejects absolute relative results, validate DB/data/artifact roots, and add independent negative tests for every gate, sibling traversal, symlink/reparse points, and cross-volume Windows paths. |
| V07-LINEAGE-009 | P1 | `apps/core/e2e/v0.7-health-test-adapters.ts:5-28`; `apps/core/e2e/daily-plan-test-bootstrap.ts:17-23,53-58`; `scripts/run-e2e.mjs:110-117`; `tests/e2e-runner.test.mjs:29-47` | The adapter computes `datasetHash` from `canonicalJson(['Fixture Food Alpha'])`, not from canonical fixture record content, so changing serving/nutrients leaves the dataset hash unchanged. No code writes the required `v0.7-health-evidence.json`; successful runs delete the runner directory, leaving no run IDs/hashes to inspect. Tests recompute neither dataset hash nor the required evidence artifact. | Dataset hash must content-address the canonical fixture records; record hash and dataset hash must be independently recomputed; the E2E runner must retain the specified redacted evidence file for Sol review. | Persisted immutable source snapshots carry a hash unrelated to most record content, while the claimed evidence file does not exist. This is a source-lineage integrity defect plus missing critical regression evidence. | Fixture lineage was reduced to a label hash, and v0.7 evidence collection was never implemented. | Define the canonical dataset preimage, compute and test both hashes from full fixture content, reject unknown synthetic lookup input, write the redacted evidence artifact, and preserve/read it before cleanup. |
| V07-CONTRACT-010 | P2 | `packages/contracts/src/fitness.ts:277-282`; `packages/contracts/src/nutrition.ts:148` | Response schemas use `z.unknown()` for disclosure, proposal, action, time request, feedback, and activity data. Supplying arbitrary extra/internal shapes is accepted by these public schemas. | The frozen contract requires strict request and response objects and regression detection for accidental internal/Owner fields. | Core response boundaries cannot validate these nested public objects, despite existing strict Proposal and calendar schemas. | Placeholder response types were left in the released public contract. | Replace every `z.unknown()` with an exact strict schema and add negative tests for extra/Owner/internal fields. |
| V07-BROWSER-011 | P2 | `apps/web/e2e/v0.7-health-loops.spec.ts:145-182,187-205`; `apps/web/tests/fitness-workspace.test.tsx`; `apps/web/tests/nutrition-workspace.test.tsx` | The focused Playwright spec passed in both contexts, but horizontal overflow is checked only once after the final Nutrition screen; Fitness and intermediate review/safety/error screens are never checked. Component coverage exercises six happy/fallback cases, not the frozen 404/409/422/429/503 and blocked-safety state matrix. | Responsive/no-overflow and visible error behavior should be proven for both workspaces and relevant stages at desktop and 390×844. | A regression in Fitness or an intermediate mobile state can pass the sole E2E. | Assertions are placed at the end of a combined loop instead of at each workspace/state boundary. | Add per-workspace/per-stage overflow and accessibility assertions plus explicit error-state component/browser tests; retain the current happy loop. |
| V07-REPORT-012 | P2 | `.agent/reports/v0.7-terra-execution.md:3-5,46-56` | `git diff --check 785b7b6..7db852f` and `git diff --check 37982d8..7db852f` both exit 1 on trailing whitespace at report lines 3-4. The handoff records a pre-report HEAD (`76257a8`) rather than the requested final target. | Final-artifact evidence should apply to the exact reviewed target and remain reproducible after the report commit. | The final committed target fails both diff-check commands even though the handoff presents those gates as green for its earlier HEAD. | Verification ran before adding the final report and was not repeated against the actual target. | Remove the whitespace and make final evidence identify and verify `7db852f`; distinguish pre-report evidence explicitly. |

## Severity counts and release decision

| Severity | Count | Release effect |
| --- | ---: | --- |
| P0 | 0 | None found. |
| P1 | 9 | Blocking: auth, persistent/idempotency, migration, fail-closed, source-lineage, and critical-proof failures. |
| P2 | 3 | Non-blocking individually, but require follow-up. |
| P3 | 0 | None recorded. |

**Final verdict: FAIL.** The minimum release condition P0=0 and P1=0 is not met.

## Commands actually run

Read-only source inspection used `git diff`, `git log`, `git status`, `git rev-parse`, `rg`, and `Get-Content` over the complete changed-file list, both frozen documents, migration v20, all new contracts/services/repositories/routes/tests, Web components/E2E, package manifests/lock metadata, and the Terra handoff. Inspection commands exited 0 except where noted below; `rg` no-match returns and two exploratory Windows-glob/path mistakes are recorded as exit 1 and did not change files.

| Command / probe | Exit | Material result |
| --- | ---: | --- |
| `git status --short --branch` | 0 | Clean target before review; branch `codex/product-prd`. |
| `git rev-parse 37982d8 785b7b6 7db852f HEAD` | 0 | Resolved the exact SHAs above; HEAD equals target. |
| `git log --oneline 37982d8..7db852f` | 0 | Inspected complete v0.7 commit chain. |
| `git diff --stat/--name-status 785b7b6..7db852f` and `37982d8..7db852f` | 0 | Established full artifact scope. |
| `rg --files` / `rg -n` / numbered `Get-Content` inspection batches | 0 generally | Read contracts, migration, implementation, tests, UI/E2E, package metadata, and report. No `AGENTS.md`/`CLAUDE.md` or `.ua/knowledge-graph.json` was present; relevant no-match scans returned 1. Two attempted Windows shell globs returned 1 and were rerun against directories. One final `Get-Content` request was user-aborted and has no exit code. |
| `npm test --workspace @ev/core -- tests/v0.7-composition.test.ts` | 0 | 1/1 passed; the test itself confirms unauthenticated capability `200`. |
| Inline unauthenticated capability injection probe | 0 | Returned `200` with three descriptors. |
| Inline local idempotency collision route probe | 0 | First `201`; changed-body same-key retry `500 INTERNAL_ERROR`. |
| Inline live-claim route probe | 0 | `409 IN_PROGRESS`, no `Retry-After`. Temporary DB was removed. |
| Inline assisted-workout replay probe | 0 | First success; retry `SQLITE_CONSTRAINT_UNIQUE`; orphan awaiting run observed. |
| Inline expired external-lease probe | 0 | Lease expired in 2020 still returned `409 IN_PROGRESS`; run remained `RUNNING`. |
| Inline decimal grammar/overflow probe | 0 | Accepted `0.0`, `0.000000`, trailing forms, and produced an out-of-range result. |
| First inline provider boundary probe | 1 | Probe fixture accidentally exceeded the 120-character field schema; corrected and rerun. |
| Corrected inline provider deadline/byte probe | 0 | Still pending after 8,250 ms; accepted a 12,331-byte strict-schema parser output. |
| First cross-Owner FK probe | 1 | Second Owner insertion hit the repository singleton constraint; rerun with the existing migration-test fixture technique. |
| Corrected cross-Owner FK probe | 0 | v20 accepted Owner A check-in → Owner B Signal. |
| Windows `path.relative` containment probe | 0 | Cross-volume outside path was absolute and accepted by the implementation predicate. |
| Catalog SHA-256 probe (`Get-FileHash` versus manifest) | 0 | Match: `cb280d075eb304b7c1127e412b70b9cfd90c62cdd410df2b622a846dfe3846dc`. |
| `npm test` | 0 | Legacy 5/5; Core 317/317; Web 246/246; Contracts 85/85; Domain 11/11. |
| `npm run typecheck` | 0 | All four workspaces passed. |
| `npm run lint` | 0 | 0 errors, 4 existing warnings in `v0.5-recovery-sweeper.test.ts`. |
| `npm run build` | 0 | Next production build passed. Its generated `apps/web/next-env.d.ts` rewrite was restored exactly; no production/source change retained. |
| `npm run test:e2e -- e2e/v0.7-health-loops.spec.ts` | 0 | 1/1 passed in 27.5 s; both desktop and iPhone contexts ran. |
| `git diff --check 785b7b6..7db852f` | 1 | Trailing whitespace in the committed Terra report, lines 3-4. |
| `git diff --check 37982d8..7db852f` | 1 | Same final-target failure. |
| `git diff --exit-code 7db852f --` package/version files | 0 | No working-tree package/version drift after verification. |
| `git status --short --untracked-files=all` before writing this review | 0 | Clean; build/E2E generated changes were restored/removed. |

Green aggregate commands are recorded as execution evidence only; they do not validate the artifact against untested frozen contracts.

## Evidence boundaries and items not run

| Evidence class | Status |
| --- | --- |
| Bundled internal exercise catalog | Local hash verified; no external provenance claim made. |
| Synthetic `TEST_FIXTURE` browser loop | Locally run; labels visible, but required durable v0.7 evidence JSON is missing (V07-LINEAGE-009). |
| Approved local nutrition dataset | **NOT RUN — APPROVAL REQUIRED** |
| Real nutrition API/source | **NOT RUN — APPROVAL REQUIRED** |
| Real health-text/DeepSeek Provider | **NOT RUN — APPROVAL REQUIRED** |
| Network, credentials, personal/health data, external datasets, installation, deployment, push, tag, release | **NOT RUN — APPROVAL REQUIRED** |

No real provider, network access, credential use/unprotect, personal data, external dataset, installation, deployment, push, tag, or release was attempted during this review.
