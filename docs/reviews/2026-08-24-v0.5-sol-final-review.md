# EV AI Dashboard v0.5 最后窄修复 Sol 终审

| 字段 | 内容 |
| --- | --- |
| 执行日期 | 2026-08-24 |
| 工作树 / 分支 | `C:\Users\asus\Documents\ev-ai assistant\.worktrees\product-prd` / `codex/product-prd` |
| 窄修复 BASE | `4b12037341c468bb612e45895e15bac4718f25ae`（`4b12037`） |
| 终审 TARGET | `0da4df6e37f121254968b09f18c7f5cbfb962a64`（`0da4df6`） |
| 审查 diff | `4b12037..0da4df6`：3 commits、6 files、`+298/-8` |
| 审查方式 | 独立 Sol，只复验剩余缺陷和最小链；不重做规划、不改产品、不拆版本 |
| Findings | **P0 0 / P1 0 / P2 1 / P3 0** |
| v0.5 工程结论 | **PASS** |
| 真实 DeepSeek | **NOT RUN — APPROVAL REQUIRED** |
| v0.6 入口 commit | `0da4df6e37f121254968b09f18c7f5cbfb962a64` |

## 1. 终审结论

最后窄修复关闭了复审剩余的 `P1-RECOVERY-001` 和 `P2-WEB-001`：

- startup/attempt-two recovery 会沿 idempotency `resourceId`、Owner-scoped preflight、Run、lease/idempotency 关联与 STARTED log 验证执行证据。合法 claim-before-business 空窗只终结 idempotency；执行已开始但关联被清空或篡改时，`buildApp` fail closed，terminal transaction 整笔回滚。
- Today Proposal decision 的 2xx body 现在必须通过 `proposalResponseSchema`，并同时关联 proposal id、严格递增 version 和 decision 对应 terminal status；malformed 或 schema-valid 但语义不一致的响应保留同一 semantic idempotency key。
- 正常 startup、重复 startup、attempt 2 terminal/replay、正常 Daily Plan generate route、Manual Event 与 v0.4 scheduling workspace focused 回归均通过。

本轮正式自动验证为 **92 passed / 0 failed**（Core 20 + Web 71 + focused browser 1）；root typecheck 四个 workspace 通过。另有两次临时 corruption mutation，各 **1 passed / 4 skipped**，不计入 92 个正式绿色测试。

按冻结判定，`P0=0`、`P1=0` 且窄修复及最小链通过，因此 **v0.5 工程 PASS**。保留的 `P2-EVIDENCE-001` 与真实 DeepSeek `NOT RUN` 均不单独阻止工程 PASS，且本报告没有把 Fake/Counting Provider 证据写成真实 Provider 成功。

## 2. 审查输入与范围纪律

已完整读取并交叉核对：

- `docs/reviews/2026-08-23-v0.5-sol-smoke-review.md`；
- `docs/reviews/2026-08-24-v0.5-sol-rereview.md`；
- `docs/releases/2026-08-24-v0.5-provider-reliability-implementation.md`；
- `4b12037..0da4df6` 的完整 6 文件 diff、3 个提交及相关现有调用链。

窄 diff 仅包含：

1. `apps/core/src/modules/daily-planning/execution-unit-of-work.ts`；
2. `apps/core/tests/v0.5-recovery-sweeper.test.ts`；
3. `apps/web/src/components/today/today-dashboard.tsx`；
4. `apps/web/tests/today-dashboard.test.tsx`；
5. `CHANGELOG.md`；
6. `docs/releases/2026-08-24-v0.5-provider-reliability-implementation.md`。

本轮没有安装依赖，没有运行全仓大规模 tests、lint 或 production build，没有访问外部网络、真实 DeepSeek 或用户数据库，也没有 commit、push、tag、release。

## 3. P1-RECOVERY-001 复验

### 3.1 合法 claim-before-business 空窗

新增文件 DB case 只建立过期 `IN_PROGRESS` idempotency claim；业务仍为 Run `CONTEXT_READY`、preflight `APPROVED`，且没有 Provider log。startup sweep 的结果为：

- idempotency 收敛为稳定 `FAILED / 503`；
- Run 保持 `CONTEXT_READY`；
- preflight 保持 `APPROVED`；
- 对应 Provider log 数量为 0；
- Provider calls = 0；
- DPAPI unprotect calls = 0；
- 第二次 startup 不再改写 idempotency `updated_at`。

这证明正常 claim-before-business 空窗口可安全终结，且重复 startup 幂等。

### 3.2 Run↔idempotency correlation 清空与篡改

TARGET 固化测试把正常 `GENERATING` Run 的 `idempotency_record_id` 清空后 close→reopen。`buildApp` 抛出 `Expired Daily Plan execution correlation is inconsistent`，并验证：

- idempotency = `IN_PROGRESS`；
- Run = `GENERATING`，correlation 仍为被注入的损坏值；
- preflight = `CLAIMED`；
- Provider log = `STARTED`；
- Provider calls = 0；
- DPAPI unprotect calls = 0。

为独立覆盖“篡改”而非仅“清空”，本轮临时把该字段改为另一 UUID，并在第二次 mutation 中对 terminal attempt 前后的四张完整行执行逐字段相等比较。两次 targeted run 均为 **1 passed / 4 skipped**；完整行比较证明 idempotency、Run、preflight、Provider log 的所有持久字段均未被当前 terminal transaction 部分改写。mutation 随后恢复，产品与测试文件均回到 TARGET 原样。

### 3.3 正常 startup / attempt 2 / repeat startup

正式 recovery sweeper tests 继续覆盖并通过：

- 正常过期 execution 在 startup 前原子收敛 Run/preflight/STARTED log/idempotency；
- 第二次 startup 不重复改写；
- 缺失 correlated preflight 时 fail closed 并回滚；
- attempt 2 request path 复用同一 terminal UoW，首次返回稳定 503，随后同 key replay 相同 body 且带 `Idempotency-Replayed: true`；
- 全部 recovery 路径 Provider=0、DPAPI unprotect=0。

实现中 `DailyPlanExecutionUnitOfWork` 只执行 SQLite statement/repository terminal 操作；没有 `fetch`、Provider `generate`、DPAPI `unprotect`、`withApiKey` 或 HTTP 调用。Provider 网络仍位于该 transaction 外。

### 3.4 P1 判定

`P1-RECOVERY-001`：**CLOSED**。

未发现新的 P0/P1。

## 4. P2-WEB-001 Today 复验

`apps/web/src/components/today/today-dashboard.tsx` 对 proposal decision 2xx response 使用 `proposalResponseSchema.safeParse`，并要求：

1. `response.data.id === proposalId`；
2. `response.data.version > input.version`；
3. ACCEPT 对应 `ACCEPTED`，REJECT 对应 `REJECTED`。

只有三项全部成立才删除 `{proposalId}:{version}:{decision}` 的 semantic key。否则先保留 key，再 refresh；若 refresh 失败，用户重试继续使用原 key。

focused Today tests 通过以下分支：

- BFF/Core 不确定 502 后重试复用同一 key；
- malformed 2xx + refresh 503 + retry 复用同一 key；
- schema-valid 但 proposal id 不同，保留同一 key；
- schema-valid 但 version 未递增，保留同一 key；
- schema-valid 但 terminal status 与 decision 不一致，保留同一 key；
- 合法 schema/id/version/status response 正常完成并刷新。

`v0.4-scheduling-workspaces.test.tsx` 与 Today 同轮 **71/71** 通过，覆盖 Manual Event 原路径及调度工作区回归；Manual Event 生产实现未出现在本窄 diff 中。

`P2-WEB-001`：**CLOSED**。

## 5. P0-P3 Findings

### P0

无。

### P1

无。`P1-RECOVERY-001` 已关闭。

### P2

- `P2-EVIDENCE-001`：**保留，不阻止工程 PASS**。focused Daily Plan E2E 仍是 hybrid browser/integration evidence：业务 fixture 和测试 credential marker 的既有边界未在本窄修复改变。它证明隔离的 Web/BFF/Core/Fake 浏览器链，不证明全部业务对象来自 Owner UI，也不证明真实 DPAPI Key 或真实 DeepSeek。

`P2-WEB-001` 已关闭，不再计入 open finding。

### P3

无。

最终分类：**P0 = 0 / P1 = 0 / P2 = 1 / P3 = 0**。

## 6. 最后 6 文件静态复核

| 轴 | 结果 | 独立证据 |
| --- | --- | --- |
| 泄密 / 敏感字面量 | **PASS** | 对新增 diff 扫描 Key/Authorization/Bearer/password/protected value 等高风险字面量，0 命中；人工复核没有 Key、prompt/completion、raw request/response 或凭据正文新增落盘。 |
| Fake 生产入口 | **PASS** | 新增 diff Fake/TestOnly/EV_E2E marker 0 命中；两个变更生产文件 marker 0 命中。测试使用 Counting Provider/SecretStore 注入，只验证“不应被调用”。 |
| 网络事务 | **PASS** | recovery UoW 的网络/DPAPI 调用模式扫描 0 命中；事务内只有本地 SQLite 查询、CAS 与 terminal write。Today 的 `requestCore` 是浏览器到本地 Core 的既有写请求，不在 SQLite transaction 中。 |
| 外部响应校验 | **PASS** | Today 把 `requestCore` 的 `unknown` 2xx payload 作为不可信输入，经 Contracts schema 和语义 correlation 后才释放 key。 |
| diff 纪律 | **PASS** | TARGET 相对 BASE 精确 6 files；无依赖、migration 或产品范围扩张。 |

## 7. 实际执行命令与数量

以下命令均在 TARGET 工作树执行。

| # | 实际命令 | Exit | 原始数量 / 结果 |
| --- | --- | ---: | --- |
| 1 | `git status --short; git rev-parse HEAD; git rev-parse 4b12037; git rev-parse 0da4df6e37f121254968b09f18c7f5cbfb962a64; git diff --stat 4b12037..0da4df6; git diff --name-status 4b12037..0da4df6` | 0 | 初始工作树干净；HEAD 精确等于 TARGET；6 files，`+298/-8` |
| 2 | `npm test --workspace @ev/core -- tests/v0.5-recovery-sweeper.test.ts tests/daily-planning-routes.test.ts` | 0 | **2 files / 20 passed / 0 failed** |
| 3 | `npm test --workspace @ev/web -- tests/today-dashboard.test.tsx tests/v0.4-scheduling-workspaces.test.tsx` | 0 | **2 files / 71 passed / 0 failed** |
| 4 | `npm run typecheck` | 0 | Core、Web、Contracts、Domain **4/4 workspaces** 通过 |
| 5 | `npm run test:e2e -- apps/web/e2e/daily-plan-review.spec.ts` | 0 | **1 passed / 0 failed**；desktop + iPhone touch hybrid path |
| 6 | 临时 tampered-correlation targeted mutation：`npm test --workspace @ev/core -- tests/v0.5-recovery-sweeper.test.ts -t "fails startup closed without partially finalizing a corrupted Run idempotency correlation"` | 0 | 执行 2 次；每次 **1 passed / 4 skipped**；第二次含四张完整行前后相等断言 |
| 7 | `git diff --check 4b12037..0da4df6; git diff --check` | 0 | 无 whitespace error；PowerShell 仅显示 clean working copy 的 LF→CRLF advisory |
| 8 | 新增 diff 敏感字面量扫描 | 0 | **0 matches** |
| 9 | 新增 diff Fake/TestOnly/EV_E2E 与变更生产文件 Fake 扫描 | 0 | **0 matches** |
| 10 | recovery UoW `fetch/requestCore/.generate/unprotect/withApiKey/http` 扫描 | 0 | **0 matches** |
| 11 | `git status --short --untracked-files=all` 与 root `.sol*/*probe*` 检查 | 0 | 写报告前 **0 status entries / 0 probe files** |

没有执行 Contracts test suite、Core/Web 全量 test、全仓 test、lint、build 或真实 Provider 调用。正式绿色自动测试合计 **92 passed / 0 failed**；临时 mutation 不计入该合计。

## 8. 真实 Provider 状态

**NOT RUN — APPROVAL REQUIRED**

本轮没有请求或读取真实 Key，没有执行 DeepSeek connection test/chat completion，没有访问 DeepSeek 网络。recovery focused tests 明确证明其路径 Provider calls = 0、DPAPI unprotect calls = 0；Playwright 使用既有 gated Fake fixture，只能计为工程自动证据。

## 9. 工程 PASS 与 v0.6 入口

冻结 gate 为：`P0/P1=0` 且窄修复与最小链通过。当前证据满足该条件，因此：

- **v0.5 工程结论：PASS**；
- **v0.6 入口 commit：`0da4df6e37f121254968b09f18c7f5cbfb962a64`**；
- `P2-EVIDENCE-001` 和真实 DeepSeek `NOT RUN — APPROVAL REQUIRED` 继续作为独立证据边界，不改变工程 PASS；
- 本报告不授权部署、push、tag 或 release，也不把 Fake 描述为真实 Provider。

## 10. 最终范围状态

本次唯一新增/修改文件为：

`docs/reviews/2026-08-24-v0.5-sol-final-review.md`

未修改两份 FAIL 报告、冻结计划、实现证据、todo、CHANGELOG 或任何产品/测试代码；未 commit、push、tag、release。临时 mutation 已全部恢复，没有遗留 probe 文件。
