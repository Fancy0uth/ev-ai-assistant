# 本地开发阶段交接（2026-09-18）

## 定位

本记录保存 `codex/product-prd` 在基线 `50b533804be24f8963492679127f72e39ff4f7dd` 后的累计本地开发状态，不是 v0.9 完整验收或生产发布声明。当前 package 版本为 0.9.0；进度唯一入口为 [TASKS](../../plans/TASKS.md)。

## 本阶段变化

- V07-LINEAGE-015 限定修复及历史证据保留；历史整版 FAIL 不改写。
- V8 实体记忆、可确认压缩/恢复、每日协调及 V9 本地运行/备份工具、访问边界和日志增量已落盘；各自限定证据见既有验收文档。
- 用户取消项目分析、项目对话、晨间项目分析及专属 Codex 集成；运行入口退役。普通工作任务、课程、健身、饮食、记忆、历史数据和既有迁移保留。没有删除个人仓库、独立 Codex 或 Docker 资源。
- Provider 设置状态改为读取实际配置元数据；最近连接测试成功不等于所有业务功能都已接通。
- 外部健身动作库目前只有 FIT-01 文字解析器通过合成检查，尚未完成版本化存储、详细训练生成和 DeepSeek 健身接线。

## 检查与限制

项目移除原证据见 [限定报告](../../plans/evidence/2026-09-16-project-removal.md)：13 Core、64 Web、1 selected 通过，Core 类型及隔离 Web 构建通过；普通 Web 类型检查曾受旧 `.next` 页面引用影响。共享 V9 浏览器检查遗漏的事件/Provider 计数断言本轮补齐，执行和独立复核结果以 TASKS 最新记录为准，未完成前不认定通过。

只使用隔离合成数据；LOCAL_RULES 是确定性规则，不是模型输出。本轮没有验证真实 Provider、物理 iPhone、长期常驻、私有 HTTPS 或生产环境，没有修改用户日常数据库。既有非本轮测试失败和未测范围保留，不以此次局部通过代替全量回归。

本轮实际结果：[R3 检查与独立复核](../../plans/evidence/2026-09-18-project-removal-r3.md) 为 1 case FAIL：任务链接选择器有歧义，随后请求取消诊断掩盖首错；关键确认前后计数未执行。断言补丁已保存，限定验收尚未通过，未执行提交或推送。

后续用户授权 R4：[修复及独立复核](../../plans/evidence/2026-09-18-project-removal-r4.md) 已限定通过。同一case在一次局部修复后1 passed、exit0，确认前后事件数量及providerCalls=0全部执行。仅精确metadata端点且有主动取消证据的最多5次挂载取消分类记录，其他网络失败仍必须为零；安全断言没有移除。本阶段达到约定的保存条件，不把上述历史FAIL改写为PASS。

## 保存边界

用户授权在限定复核和敏感检查之后，提交并推送当前累计源码、测试和文档至既有开发分支；不合并 main，不创建标签或 Release。不纳入 `.env`、数据库、数据目录、构建结果或 Python 编译缓存。启发式密钥检查不是完整秘密审计。

提交前暂存核对：150个变更路径（140个新增/修改文本，10个删除），暂存内容启发式密钥扫描无命中，禁止的数据/凭据/缓存路径无命中；远端分支仍为基线50b5338。完整 `git diff --cached --check` 报告历史 evidence 的机械diff上下文空白及一份历史审查末尾空行；排除 `plans/evidence/**` 后源码与正文检查无输出。历史证据按要求原样保留，此处明确记录格式限制，不宣称完整空白检查通过。

## 后续

健身按已批准 FIT-02 → FIT-03 → FIT-04 推进：版本化动作目录及候选审核、详细训练草案和反馈上下文、DeepSeek 接线及训练详情。真实健康数据和收费 API 验证另需明确授权。启动说明见 [DEPLOYMENT](../DEPLOYMENT.md)，无预设账号；不要将日常库作为测试库。远程手机访问和课程真实视觉/检索仍是后续事项，不恢复项目分析。
