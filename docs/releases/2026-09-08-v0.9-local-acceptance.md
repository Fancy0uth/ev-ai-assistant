# v0.9 本地实现与运维验收记录

> **LOCAL_IMPLEMENTATION_PASS（限定本地）；整体 PARTIAL。** 最终Astra medium限定复核无有效P0/P1。唯一进度在 [TASKS](../../plans/TASKS.md)，此文件为验收交接，不是第二套任务账本。本地结果不等同于完整MVP、真实Provider或生产验收。

## 版本与环境

- 版本：v0.9.0「Private iPhone and Operations」；4个workspace及根目录共5份manifest、lockfile内部依赖与APP_VERSION一致。SQLite schema仍为26，V9未新增或改写迁移SQL。
- 工作区：`C:\Users\asus\Documents\ev-ai assistant\.worktrees\product-prd`，分支`codex/product-prd`，基准HEAD `50b533804be24f8963492679127f72e39ff4f7dd`。本轮包含未提交修改，不以HEAD独自证明代码状态。
- Windows；Node24.18.0；主工具PowerShell7.6.5。依赖预先存在，本轮未安装或更新依赖。
- 实际协作：Terra Max实现，Astra medium初始文档与少量聚焦复核。未宣称使用不受工具控制的速度档位。

## 本轮交付范围

1. 固定Web Origin、CSRF、安全头和Next nonce接线；保留Owner认证与Proposal确认边界。
2. Windows受管启动和WhatIf安装/卸载脚本；真实计划任务注册和重启另行授权。
3. SQLite一致备份及只读校验、独立新目录恢复演练；不是包含附件/密钥环境的全备。
4. 本地脱敏有界日志、匿名最小live/ready和Owner详细健康状态。
5. 一条隔离Chromium浏览器主路径及受影响类型/构建检查；另有只读配置/监听检查工具，不修改网络设置。

## 验证边界

所有程序测试仅用本次合成数据、临时目录及loopback。Mock仅验证脚本适配器，不证明真实Windows启动/重启；LOCAL_RULES是静态规则，不是真实AI。真实Provider、个人项目、日常Owner数据、付费资源及外部网络均未使用。

## 实际最小检查

以下由Terra Max实际执行，主Agent核对最后修改顺序和代码指纹；未重跑全仓矩阵。

| 检查命令（仓库根） | 最终结果 | 证据边界 |
| --- | --- | --- |
| `node --test tests/windows-launcher.test.mjs` | exit0，1/1 | PowerShell parser、Mock进程/任务和WhatIf；不是真实自启 |
| `npm test --workspace @ev/core -- tests/auth.test.ts -t "rejects logout without a valid Owner session"` | exit0，1通过、9按名称过滤 | logout实际Owner保护；不是全auth suite |
| `npm test --workspace @ev/core -- tests/v0.9-backup-restore.test.ts tests/v0.9-operations.test.ts` | exit0，2文件2/2 | 合成SQLite备份/校验/新目录恢复；日志、健康及失败清理 |
| `npm test --workspace @ev/web -- tests/v0.9-private-boundary.test.ts` | exit0，1/1 | Origin/CSRF与BFF权限组合场景 |
| `node scripts/run-e2e.mjs 'v0.9-private-iphone.spec.ts'` | exit0，Chromium单worker、1/1 | 实际本地HTTP浏览器主路径，不是物理iPhone |
| `node --test tests/v0.9-version.test.mjs` | exit0，1/1 | 五份manifest、内部依赖、lockfile及APP_VERSION |
| `npm run typecheck --workspace=@ev/contracts --workspace=@ev/core --workspace=@ev/web` | exit0 | 三个受影响workspace，不是全量行为测试 |
| `$env:NEXT_TELEMETRY_DISABLED='1'; npm run build --workspace @ev/web` | exit0 | Web构建，不是HTTPS生产部署 |
| `& .\scripts\windows\check-private-access.ps1 -WebOrigin 'http://127.0.0.1:3217' -CoreHost '127.0.0.1' -CorePort 4327 -WebPort 3217` | exit0，两端口NOT_LISTENING | runner已停止自身进程；不代表远程可访问 |

浏览器闭环：setup → Today → 本机CLI授权合成项目 → LOCAL_RULES Brief → 确认Action/TimeRequest → LOCAL_RULES协调Proposal → 确认 → 刷新Today。确认前不提前写入行动/日程；外部Provider调用0。场景断言CSP/nonce、console/page/API及外部请求诊断无异常。精确V9 selector使用正常Core入口；仅V8/V9精确单文件场景不要求未执行的V07 fixture证据，其他selector保留原门禁。测试使用独立EV_DATA_DIR及127.0.0.1:3217/4327，结束停止自有进程；没有启动用户预览。

最终顺序为browser通过 → Core旧版本期待修正 → Core两用例 → Web一用例 → 版本一用例 → 三workspace类型 → Web构建 → 只读监听检查。最后修改只影响Core测试版本期待，因此最终证据覆盖相关修改。Windows脚本未在后续修改，复用其有效单场景证据。

收口保留7组修复：lockfile路径映射、backup严格类型、Web测试严格类型、Core runner启动环境、Next TS配置加载、UI locator限定、operations版本期待。共9次命令exit非零（版本1、类型1、browser6，其中5次启动失败与1次locator失败、Core1），另1次只读模块解析探针非零；不是9个新增缺陷，也不是9次成功。此前01～04失败/修复记录保留在TASKS，不以最终绿覆盖历史。

## 验收操作入口

精确启动配置和参数以 [DEPLOYMENT](../DEPLOYMENT.md) 为权威正文；接口和错误以 [API](../API.md) 为准。普通本地HTTP预览走dev，生产launcher要求显式HTTPS Origin，不会替用户建立私有网络。

系统没有预设账号和密码。全新隔离数据目录需由使用者在setup创建唯一Owner；测试用合成账号不属于可持续使用的预览凭据。浏览器地址必须与配置的Web Origin一致。

## 尚需单独授权或实机证据

- 认证私有HTTPS反向代理，仅代理Web，Core不得公开。
- 物理iPhone Safari实际访问、登录、Proposal确认、刷新与断线恢复。
- 当前用户计划任务注册、异常停止与主机重启后的真实自启。
- 真实数据的完整恢复验证，包括artifacts引用与DPAPI用户环境。
- 继承的真实Provider证据债。没有真实用户反馈时标待收集。

以上未运行就保留NOT RUN；缺少必需实机证据时整体只能PARTIAL，不称完整v0.9 PASS。

未运行全仓回归、多浏览器/设备矩阵、生产HTTPS nonce、WebKit（本机未安装，未补装）、真实Windows进程生命周期与重启、真实Provider、真实数据恢复。已知P2包括启动失败时“lock retained”可能不准确（不得凭提示删锁或认定进程已停），以及继承的UTC默认日期、Brief分页和历史问题，详见[ROADMAP](../../plans/ROADMAP.md)。SQLite-only备份不包含artifacts、记忆投影或DPAPI用户环境，不能称整机全备。

## 停止、回退与发布

最终复核另保留P2-E2E-LOG-ENV：runner继承父环境的EV_LOG_DIR，未来测试前必须确保它未设置或指向隔离目录，避免合成日志混入日常日志。未观察到本次实际污染；不是日志隔离全场景通过声明。当前唯一下一步是取得真实私有部署与实机验收的精确授权。

不按端口或进程名清理陌生进程，不删除日常数据。备份恢复仅进入新隔离目录，不覆盖活跃库；代码回退不等于数据库降级。真实停服换库必须先验证一致备份并另获授权。

当前V8/V9成果未提交，因此没有本轮创建的可直接checkout的版本发布点。回退前需保存当前未提交成果，再由用户选择并授权代码基线和独立数据副本；不要把基准HEAD当作包含这些成果的回滚点。V9没有新增迁移SQL，但这不等于授权直接换回旧代码操作日常库。

本轮未stage、commit、push、tag、Release或生产部署；不自动进入后续版本。剩余风险与唯一下一步最终以TASKS收口内容为准。
