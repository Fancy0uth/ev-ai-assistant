# v0.8 本地限定验收与交接

日期：2026-09-08。此文是本轮冻结交接记录，不作为第二套进度；后续状态唯一维护于[plans/TASKS.md](../../plans/TASKS.md)。

## 结论与代码状态

**V8-01～05实现完成、最小跑通通过、限定独立复核通过；真实依赖未验证、生产未部署、版本未发布。**

- 仓库工作区：`C:\Users\asus\Documents\ev-ai assistant\.worktrees\product-prd`。
- 分支：`codex/product-prd`；HEAD：`50b533804be24f8963492679127f72e39ff4f7dd`。交付为该HEAD上的未提交工作区，不能用HEAD本身代替当前实现证据。
- 应用及根/四个workspace/lock内部版本一致为`0.8.0`，health与run复用APP_VERSION。没有stage、commit、push、tag或GitHub Release。
- 实现：实际gpt-5.6-terra/max；限定独立复核：实际gpt-6-astra/medium。修复次数与历史FAIL完整保留于TASKS，没有换模型或任务名清零。
- V07-LINEAGE-015限定PASS仍有效：营养repository/service/定点测试SHA256与原[独立复核](../reviews/2026-09-08-v07-lineage-015-focused-review.md)一致；原v0.7整版FAIL不改写。

## 本版可操作闭环

| 范围 | 可见能力与边界 |
| --- | --- |
| V8-01 本机授权 | CLI向EV库授权现有目录；Web只能选项目ID，不提供任意本机路径登记 |
| V8-02 项目 | 根兼容及docs/plans白名单受限读取；静态Brief展示依据、下一步、阻塞/工作量；确认后创建Action/TimeRequest，不写原项目 |
| V8-03 实体记忆 | 保留旧四域，增加项目/课程/训练/饮食/每日实体；按Owner与实体隔离，历史与Markdown投影可追溯 |
| V8-04 记忆草案 | 手动/阈值形成有界LOCAL_RULES去重草案；展示来源、预算和差异；确认/恢复追加版本，拒绝不改正文，永久删除不可复活 |
| V8-05 协调 | 汇总既有多领域TimeRequest；同Owner/日期一份活跃协调草案，替换旧候选为STALE；确认才写日程，版本/幂等避免重复与过期覆盖 |

LOCAL_RULES是确定性静态分析/排程，不是模型推理。测试替身只用于合成边界测试；外部Project/Compaction/新协调入口未配置时返回503，不以假回复表示接入成功。没有用真实API Key、个人项目、课程或健康数据验证。

## 最小测试记录

环境：Windows、Node24.18、既有TypeScript/Next/Fastify/SQLite依赖；未安装依赖。命令均在上述worktree根执行。下列最终命令由Terra实际运行；主Agent检查最终diff、复核hash和Playwright结果，不重复运行旧测试矩阵。

| 检查 | 实际命令 | 最终结果 |
| --- | --- | --- |
| 页面竞态 | `npm test --workspace @ev/web -- tests/v0.8-memory-scope.test.tsx tests/v0.8-project-scope.test.tsx` | exit0，2文件2用例通过 |
| Core三链 | `npm test --workspace @ev/core -- tests/v0.8-project-cli.test.ts tests/v0.8-project-brief.test.ts tests/v0.8-entity-memory.test.ts tests/v0.8-memory-compaction.test.ts tests/v0.8-coordination.test.ts` | exit0，5文件5用例通过 |
| 类型 | `npm run typecheck --workspace=@ev/contracts --workspace=@ev/core --workspace=@ev/web` | exit0，3 workspace通过 |
| 构建 | `npm run build --workspace @ev/web` | exit0，NEXT_TELEMETRY_DISABLED=1 |
| 浏览器 | `node scripts/run-e2e.mjs 'v0.8-project-memory-coordination.spec.ts'` | exit0，同一场景1/1通过；关闭遥测 |
| 空白差异 | `git -c core.safecrlf=false diff --check` | exit0，主Agent亦核对 |

有效RED包括两页面旧实现下的两个失败；Core测试证明v22→26合成旧记忆可读、删除保护、项目字节不变、确认/重复/过期保护。没有零用例成功或删除原断言来制造通过。

浏览器为单Chromium、1440×900、本机Web3217/Core4327、runner独立EV_DATA_DIR。真实页面操作覆盖：新建合成Owner→CLI授权合成项目→Brief/Action确认→记忆压缩草案确认→静态协调并采用安排。两页面A→B→A竞态另由两个组件用例验证，不声称所有异步排列或Safari/手机均已覆盖。

唯一预期诊断为新Owner的空GENERAL记忆GET404：精确路径/文案、实际响应存在、诊断数量不超过响应数；其他console warning/error/pageerror仍失败。不是绝对零诊断或所有HTTP均200。

E2E曾通过但runner因要求未运行的V07证据而exit1；只对精确单V8 selector免除该次V07归档，其他调用仍保留原强制验证，并采用真实Playwright退出码。最终[Playwright结果](../../apps/web/test-results/.last-run.json)为passed、failedTests为空；该生成文件可能被后续运行替换，冻结结论及源码hash见TASKS。

## 聚焦审查与日志

- 两处P1已修：记忆/项目异步旧响应不再污染新范围；身份+对象代际覆盖A→B→A、成功/失败/finally、加载/保存/草案/决定/恢复，操作前检查当前成员及归属。Astra分别判ADDRESSED，最终四文件hash与复核一致。
- 追加迁移26只约束daily_plan_proposals的活跃owner/date，不限制其他Proposal类型；两条既有完成路径均保留事务边界。
- 最后仅补核runner、404诊断和Today模式透传，无新增P0/P1，未重开整版审计。四类TypeScript集成错误已修复。
- 同一协调用例断言日志的request/run/proposal ID、mode、状态、耗时与skips，且不含合成正文、密码、会话凭据。项目跳过原因在EV的Brief证据中；没有新建监控平台或声称长期运维已验证。

## 体验与启动

真实启动命令、非敏感配置、CLI授权、数据位置和停止方法唯一维护在[DEPLOYMENT](../DEPLOYMENT.md)。简要路径：

1. 从worktree启动两个终端：独立`EV_DATA_DIR`下执行`npm run dev:core`，另一个执行`npm run dev:web`；BFF的EV_CORE_URL固定loopback Core。
2. 浏览器打开`http://127.0.0.1:3000/setup`，自行建立唯一Owner；**没有默认账号或预览密码**。本轮测试账号仅用于已清理的合成run，不是可复用登录凭据。
3. 本机CLI授权目录后进入`/projects`，确认Brief行动；进入`/memory`审阅草案；到`/daily-plan?date=YYYY-MM-DD`协调并确认，再从Today查看。

本轮没有为用户保持运行的日常预览。3217/4327测试服务已停止，成功run合成目录由runner清理；失败诊断目录保留，未删除用户数据。

## 已知限制、回退与唯一下一步

- P2：Brief列表未分页；项目及DAILY默认日期使用UTC截取，非UTC凌晨需人工核对；404诊断仅总数匹配而非逐请求配对。既有v0.6/v0.7 P2继续保留，见[ROADMAP](../../plans/ROADMAP.md)。
- 压缩减少有效摘要/后续上下文，不删除历史，不承诺SQLite磁盘永不增长。真实模型质量、真实个人数据、全仓test/lint、全历史迁移、多设备/手机、性能/压力、生产环境均未验证。
- 不回退或清空用户数据库。已追加迁移的库不能靠切回旧代码假定兼容；回退只在另获授权的独立副本和一致备份上处理，参见DEPLOYMENT。Docker、私有iPhone HTTPS、Windows常驻和WAL备份未交付。
- **唯一下一步：用户按独立数据目录体验这三条链，记录反馈。** 本轮工作结束，不自动生产部署、提交发布或进入v0.9；没有真实用户反馈就不编造效果提升。
