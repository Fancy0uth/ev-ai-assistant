# 2026-09-19 健身闭环阶段交接

## 结论

**实现已接通，业务路径断言到达终点；浏览器整体验收未通过。** 不能作为完整PASS或正式发布。当前唯一进度与下一步见[任务清单](../../plans/TASKS.md)。

工作区`.worktrees/product-prd`，分支`codex/product-prd`，HEAD `2634a289a68891e0fb58649838829e1c2f8c81b4`加本轮未提交修改；版本仍0.9.0。没有新commit、push、tag、Release或生产部署。

## 本轮实现与修复

- 动作目录离线导入、不可变来源及独立资格；真实外部数据库未下载/导入，自动适用性未授权。
- V2上下文、三阶段详细训练、DeepSeek适配器及已认证HTTP装配；无配置明确不可用，不默认返回Fake。
- 详细训练页面及旧手动入口兼容，实际反馈输入、独立疼痛警告、外发预览与显式同意。
- 新身体状态使旧依据失效；重复保存同样状态不错误重放旧记录。
- 反馈事实先提交，再更新有界本地记忆；人工编辑/删除重建得到保护，冲突或投影失败显示降级。
- 训练确认只生成Action/TimeRequest，日程另经确认生成Event。

业务代码、共享契约/领域逻辑、迁移28～30、相关小测试及专用E2E基础设施已落盘。规范文档更新包括API、DATABASE、TECH_SPEC、ARCHITECTURE、DEPLOYMENT、ROADMAP和TASKS；历史失败证据保留。

## 实际验证

| 层级 | 结果与范围 |
| --- | --- |
| 目录、契约、上下文、适配器 | 各自既有定点用例及限定复核；具体命令/hash见TASKS引用 |
| 服务闭环03d | R2单case 1/1；真实第二连接合成删除重建反例修复，限定复核PASS |
| 页面04b | R2单Mock case 1/1，原两项UI缺陷复核PASS |
| 路由03e | R1单inject case 1/1，鉴权/未配置/注入门禁及包装复核PASS |
| 类型 | Core现有配置加专用bootstrap共169入口，0诊断；不是执行169个测试 |
| Web构建 | 独立源码副本build exit0；旧.next缓存未清理。副本首轮漏配置的失败保留 |
| 最终浏览器04c | 1case失败；业务断言完成，诊断门禁未通过，无成功归档 |

集中检查记录：[隔离验证](../../plans/evidence/2026-09-19-fit-web-isolated-validation.md)。最终浏览器记录：[执行报告](../../plans/evidence/2026-09-19-fit04c-final.md)。构建后04c选择器/诊断测试文件又有变更，不把先前build当作这些最终测试改动的类型证明。

R2浏览器通过的中间断言：两次Fake调用；确认前事实0/0/0，训练确认后1 Action/1 TimeRequest/0 Event，独立静态排程确认后1 Event；保存一份合成反馈，记忆RECORDED；第二次授权上下文含该反馈和所选记忆，第二份训练保持DRAFT，最终事实仍1/1/1。这些中间结果不能替代最后诊断与归档门禁。

## 未关闭项

1. 9次GET `ERR_ABORTED`，涉及Provider配置元数据及详细训练profile/capability/memory。生命周期取消是待验证解释，当前证据不能直接全部豁免。
2. 专用Web网络保护写出`denied=true`，但没有目标、计数或原因；尚不能确定被阻止的请求来源。保护未关闭，Core外连拒绝计数0，浏览器外部请求计数0；这些数字不能证明Web子进程没有尝试外连。
3. 缺少成功browser证据文件与最终归档；04c修复预算2/2耗尽，未继续重跑。

## 操作路径与数据边界

日常界面目标路径：`/fitness`→“DeepSeek 详细训练计划”→保存画像和当天状态→审阅候选/实际外发内容→同意生成→训练提案确认→`/daily-plan`独立排程确认→反馈→再次规划。真实执行需要另行授权使用用户密钥、费用及健康数据，本轮未做真实效果测试。

本轮唯一浏览器命令：`node scripts/run-e2e.mjs fitness-planning-loop.spec.ts`，专属Web3217/Core4327及managed-run合成目录；当前失败运行已停，两个端口无监听，失败证据保留。未创建供日常使用的新测试账号，也没有启动新的长期预览。启动配置与回退边界见[DEPLOYMENT](../DEPLOYMENT.md)，不得把测试Fake启动入口用于日常运行。

无全仓测试、全迁移矩阵、跨设备矩阵、真实DeepSeek调用、真实外部动作审核或生产验证。没有新的真实用户反馈，不宣称健身效果或效率改善。

下一步只处理上述04c验收阻塞，需额外限定授权；不新增功能，不恢复项目分析模块，不改写历史FAIL。

## 独立复核补记

Astra medium Planck只读核对实际合成库、Fake原件、四文件与R2日志hash，确认中间业务证据成立且成功artifact/归档不存在，最终NOT PASS。代码中有对应AbortController清理机制，但没有逐请求配对证明9次取消全部属于预期。Web拒绝标记证明发生拒绝，不证明外网成功，也不能定位原因。

建议额外一次限定取证/修复：记录四类GET的有界请求身份与取消/完成配对；网络guard保留拒绝并记录脱敏参数形态、进程角色、目标分类和计数。禁止按路径或ERR_ABORTED直接扣除、开放未知目标或放宽事实/归档门禁。同一合成case最多再运行一次；证据仍不足就维持NOT PASS，真实API和个人数据仍不在授权内。
