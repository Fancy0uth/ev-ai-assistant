# EV AI Dashboard v0.5 Provider Reliability — 实现与自动验收证据

| 字段 | 内容 |
| --- | --- |
| 日期 | 2026-08-24 |
| 工作树 | `C:\Users\asus\Documents\ev-ai assistant\.worktrees\product-prd` |
| 分支 | `codex/product-prd` |
| 实现基线 | `561bceb` |
| 版本事实 | `0.5.0` |
| 实现范围 | Provider Reliability；不包含 v0.6+ 产品功能 |
| 发布状态 | 未 push、未 tag、未 release |
| 真实 DeepSeek | **NOT RUN — APPROVAL REQUIRED** |

## 1. 本版交付

v0.5 把 Daily Plan 的 Provider 调用从“可调用”收紧为可恢复、可审计、默认失败关闭的本地链路。

```text
Owner action
  -> Web semantic correlation + Idempotency-Key
  -> BFF request/response header allowlist
  -> Owner-scoped idempotency record and lease claim (SQLite short transaction)
  -> provider call outside SQLite transaction
  -> frozen model/response policy + quota/timeout checks
  -> matching lease token finalizes redacted metadata and response snapshot
  -> replay returns the original safe response; mismatch fails with 409
```

已实现的边界：

- SQLite v18 仅加法：`idempotency_records`、`provider_call_logs` 和 Daily Plan Run 的 lease/attempt/deadline/terminal metadata；迁移测试覆盖既有业务数据与 credential metadata 保留。
- `POST /v1/daily-plans/generate`、`POST /v1/daily-plans/proposals/:proposalId/decisions`、`POST /v1/proposals/:id/decision` 都强制 Owner-scoped `Idempotency-Key`。
- 同一语义请求 replay 首次安全响应；同 Key 不同 operation/resource/body hash 返回 `409 IDEMPOTENCY_CONFLICT`；有效执行中的同 Key 返回 in-progress 语义；decision 的 claim/业务状态/响应快照在单一 SQLite transaction 中处理。
- DeepSeek adapter 只接受冻结的 endpoint、`deepseek-v4-flash`/`deepseek-v4-pro`、非流式结构化响应和 allowlisted `finish_reason`/usage；未知、截断、工具调用、超时、配额或不完整响应均失败关闭。
- Provider 调用不在 SQLite transaction 内；lease token 阻止晚到响应，至多恢复一次，第二次失效收敛到稳定终态。持久日志只保存 policy 允许的计数、状态、模型、日期、耗时和本地错误码，不存 Key、Authorization、prompt、completion 正文、原始 request/response。
- Web 对 prepare/approve/generate 建立 id/run/localDate/version/payload/action 关联。schema 合法但语义不一致时停止，不继续生成或写入。
- 版本、health、package、run 和 Provider log 的应用版本统一为 `0.5.0`。

## 2. 自动验证结果

| 检查 | 结果 | 证据 |
| --- | --- | --- |
| v17 -> v18 无损迁移、reliability repositories | PASS | 聚焦 Core migration/repository 测试已通过；全仓 Core 253 tests 通过。 |
| 三入口 idempotency、Owner isolation、decision atomicity | PASS | Core route/service 契约测试通过；浏览器测试复核 generate replay/conflict。 |
| lease/recovery/late response、timeout/quota、fail closed | PASS | `v0.5-provider-lease`、provider policy、Daily Planning service 和 adapter 聚焦测试通过。 |
| DPAPI port 与 credential 脱敏 | PASS | Provider credential/service/route/connection 聚焦测试通过；本轮未在命令、日志、快照或 Git 中使用真实 Key。 |
| Web action correlation/BFF header allowlist | PASS | Web hook/BFF 聚焦测试通过；浏览器用例走 prepare -> approve -> generate。 |
| 全仓自动测试 | PASS | `npm test`：71 files / **557 passed** / 0 failed。 |
| Workspace typecheck | PASS | Core、Web、Contracts、Domain 全部通过。 |
| lint | PASS | `npm run lint` 无诊断。 |
| Web production build | PASS | `npm run build --workspace @ev/web`；Next 16.3.0 成功生成 18 条路由。 |
| Focused browser E2E | PASS | 2/2 passed：Daily Plan desktop + iPhone touch，Provider settings desktop + 390x844 + 320x800 touch。 |
| 静态边界扫描 | PASS | 所有 package version 为 0.5.0；生产 `apps/core/src`、`apps/web/src` 无 Fake/TestOnly E2E symbol；`provider_call_logs` schema 无 raw request/response、Authorization、Key 或正文列。 |
| whitespace | PASS | 最终 `git diff --check` 通过。 |

全仓验证期间曾发现一条 Contracts fixture 未显式声明既有默认字段 `scheduling: null`，导致 input 与 schema-normalized output 的深度相等失败。根因已按最小范围修复并由 `tests/contracts.test.ts` 25/25 和随后的全仓 557/557 回归测试关闭；没有修改生产契约。

## 3. 浏览器证据与测试 fixture 边界

每日计划浏览器 E2E 使用 runner 自己创建和清理的 `EV_DATA_DIR`、端口 4327/3217、唯一测试数据及测试依赖注入的 Fake Provider。实际覆盖：

1. Owner 从 Setup/Login 页面进入；
2. Daily Plan 走“准备外发内容 -> 批准外发内容 -> 调用 Provider 生成草案”；
3. 同一浏览器请求的同一幂等键 replay 返回 `Idempotency-Replayed: true`；相同键配不同语义请求返回 `409 IDEMPOTENCY_CONFLICT`；
4. 未配置 Provider 返回安全失败；草案部分采用、刷新及日程版本过期路径仍可用；
5. iPhone touch 视口检查触摸目标、无横向溢出与浏览器 console/page error；
6. Provider 设置页在桌面、390x844、320x800 下保持未配置状态，Key 输入为 password 类型，且不会自动触发连接测试。

`apps/core/e2e/daily-plan-test-bootstrap.ts` 会在上述隔离测试期间直接写入一个非真实 credential marker，以使 Fake Provider 可被确定性调用。它是 **out-of-band 测试基础设施**：不证明 Owner UI -> Core -> DPAPI 的生产凭据路径，也不证明真实 DeepSeek 请求。DPAPI port、credential service 和响应脱敏由单元/集成测试覆盖；真实 Key 和真实网络调用没有发生。

## 4. 原子提交链

1. `dc6992b` `feat(v0.5): freeze reliability contracts and version`
2. `75f28f6` `feat(core): add v18 reliability storage`
3. `a47d9bb` `feat(core): enforce idempotency on high impact routes`
4. `39b4ff9` `feat(core): harden provider execution and recovery`
5. `c79806c` `fix(web): correlate preflight actions and idempotent retries`
6. `aa1cd0b` `feat(providers): expose safe reliability settings and status`
7. `7c2f1ac` `test(v0.5): cover provider reliability smoke paths`
8. `ab5fcae` `test(contracts): align task conflict fixture`

本文件、Changelog 和任务账本将由最后一个文档提交收口。

## 5. 已知状态、非目标与回滚

### 未解决 P0/P1

无。

### P2/P3 与外部审批

- 本版没有新增未解决 P2/P3；v0.4 已记录的可访问性/CSS 维护性项仍按路线图处理，未在可靠性版中扩张。
- 真实 DeepSeek 人工验收是唯一外部审批门：**NOT RUN — APPROVAL REQUIRED**。在用户通过 Owner UI 提供 Key 并明确批准一次最小请求前，不能将 Fake 自动结果说成真实 Provider 成功证据。

### 明确非目标

未实现 v0.6+ 的课程截图、公开检索、健身/饮食 RAG、项目读取、长期记忆协作、远程 iPhone 部署或新外部服务；没有安装依赖、执行真实 DeepSeek 请求、读取/迁移用户真实数据库、push、tag 或发布。

### 回滚

以原子提交逆序 `git revert`。v18 为加法迁移，无自动 down migration；若数据库已升级，回滚应用代码时不得手工删除 v18 表或列，应先停服务并使用升级前的一致 SQLite 备份恢复。

## 6. 给后续最小审查的输入

后续审查只需核对冻结的 v0.5 目标：三入口幂等/Owner 隔离、lease/recovery、fail-closed、DPAPI/脱敏、Web correlation、版本一致性与上述自动证据。它不应重新规划 v0.6+，也不得将真实 DeepSeek 状态由 `NOT RUN — APPROVAL REQUIRED` 改写为 PASS。
