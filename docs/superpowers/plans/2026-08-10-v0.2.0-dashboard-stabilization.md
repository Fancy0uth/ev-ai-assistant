# EV AI Dashboard v0.2.0 Dashboard 稳定化实施计划

> 本计划执行 `2026-08-10-v0.2.0-improvement-design.md`。每个编码任务由新的 `gpt-5.6-terra`（`reasoning_effort=xhigh`、默认服务速度）在隔离 fork 中按 TDD 实现；随后由新的 `gpt-5.6-sol`（`reasoning_effort=xhigh`、`service_tier=priority`）只读复审。主 Agent 冻结契约、集成提交、运行全套门禁并处理审查分类。

## 1. 执行规则

- 基线：`v0.1.0` / `83f4b37`；实现分支：`codex/v0.2.0`。
- 结构性编码前后分别运行 Code Intel Sentrux `session_start` / `session_end`。
- 每个任务遵循：上下文包 → RED 测试 → 确认按预期失败 → GREEN 最小实现 → 聚焦验证 → Terra 自审 → 独立提交 → Sol 新上下文反方审查 → 同一 Terra 修正 → Sol 复审。
- 不并行运行实现 Agent；相邻任务可以读前序提交，但不得改写其他 Agent 的未提交工作。
- 每个任务候选写入范围约不超过五个文件。若实现证明必须扩张，先由主 Agent拆成更小任务，不能把重构夹带进当前提交。
- 不修改 migration 001，不更新 Code Intel baseline 隐藏回归，不读取或清理用户现有数据目录。
- 所有 API 错误保持 `{ error: { code, message, details? } }`；新增接口全部在 `/v1` 下并由共享 Zod 契约验证。
- 下列命令中的 workspace 聚焦测试先通过，任务才允许提交；最终仍需运行全仓门禁。

## 2. 已完成前置条件

- [x] 私有 GitHub 技术预览 `v0.1.0` 已从干净提交 `83f4b37` 发布。
- [x] 基线 `npm test`、typecheck、lint、build、E2E、`git diff --check` 与 Code Intel lite 已执行并通过；问题已进入改进规格。
- [x] 本计划已由主 Agent 复核并冻结，Sentrux `session_start` 已通过。

## 3. 实施任务

### Task 1：冻结 Agent 公共契约

候选文件（3）：

- `packages/contracts/src/agent.ts`（新建）
- `packages/contracts/src/index.ts`
- `packages/contracts/tests/contracts.test.ts`

RED：

- 为 `AgentCapability`、`AgentSession`、`AgentMessage`、session/message 分页、创建 session、发送 message 的输入输出 schema 写失败测试。
- 断言 message 空白、超过 8000 字符、session title 超过 80、pageSize 超过 100、未知字段会被拒绝。
- 断言发送成功响应必须同时含服务端 USER 与 ASSISTANT 消息。

GREEN：只新增 `agent.ts` 并从 index 导出；不写数据库或 Provider 实现。

验收：四组 API 的所有输入/输出都可由共享 schema 表达，类型不含供应商专有字段。

验证：

```text
npm test --workspace @ev/contracts
npm run typecheck --workspace @ev/contracts
```

独立提交：`feat(contracts): define provider-neutral agent resources`

### Task 2：扩展 Task 查询与冲突详情契约

候选文件（3）：

- `packages/contracts/src/tasks.ts`
- `packages/contracts/src/api.ts`
- `packages/contracts/tests/contracts.test.ts`

RED：

- 证明 `area`、`status`、精确 `targetDate`、`dateScope=FUTURE|UNDATED`、`referenceDate` 的有效组合。
- 证明 `targetDate` 与 `dateScope` 互斥，FUTURE 缺 referenceDate、UNDATED 带 referenceDate 都返回验证失败。
- 为 409 `details.currentTask` 和 Web 可读取的 typed error details 写契约测试。

GREEN：对现有 query/error schema 做加法扩展；保留旧 `targetDate` 调用兼容。

验收：旧 Task 客户端仍能解析；新筛选组合与冲突最新实体有唯一、明确语义。

验证：同 Task 1。

独立提交：`feat(contracts): extend task filters and conflict details`

### Task 3：新增 SQLite migration 002 与真实升级测试

候选文件（2）：

- `apps/core/src/storage/migrations.ts`
- `apps/core/tests/database.test.ts`

RED：先用冻结的 migration 001 SQL 建库并写入 Owner、session、Task，再通过 `openDatabase` 升级；断言 Agent 表当前不存在或测试失败。

GREEN：在 migrations 数组末尾追加 002，创建 `agent_sessions`、`agent_messages`、外键与索引。严禁改 migration 001。

验收：001→002 后旧记录逐字段不变；002 只记录一次；重开幂等；session/Owner 级联和 Agent 外键有效。

验证：

```text
npm test --workspace @ev/core -- database.test.ts
npm run typecheck --workspace @ev/core
```

独立提交：`feat(core): add additive agent storage migration`

### Task 4：实现 Owner 隔离的 Agent Repository 与 Service

候选文件（4）：

- `apps/core/src/modules/agent/provider.ts`（新建；Provider Port 在 Service 依赖出现时即定义）
- `apps/core/src/modules/agent/repository.ts`（新建）
- `apps/core/src/modules/agent/service.ts`（新建）
- `apps/core/tests/agent-service.test.ts`（新建）

RED：覆盖 session 创建/分页、message 分页、其他 Owner 按 404 处理、无 Provider 在任何写入前报 `AGENT_PROVIDER_NOT_CONFIGURED`、Provider 失败零写入、成功时事务写两条消息。

GREEN：Repository 只做 SQLite 持久化；Service 接收可选 Provider Port，先生成候选 user message，在 Provider 成功后事务写入两条消息。

验收：Fake Provider 只从测试注入；Service 不导入 DeepSeek/Codex SDK；其他 Owner 无法枚举或访问 session/message。

验证：

```text
npm test --workspace @ev/core -- agent-service.test.ts
npm run typecheck --workspace @ev/core
```

独立提交：`feat(core): add isolated agent session service`

### Task 5：接入四组 Agent API 和 Provider Port

候选文件（5；复用 Task 4 已冻结的 Provider Port）：

- `packages/contracts/src/agent.ts`（补充严格 session 路径参数 schema）
- `packages/contracts/tests/contracts.test.ts`
- `apps/core/src/modules/agent/routes.ts`（新建）
- `apps/core/src/app.ts`
- `apps/core/tests/agent-api.test.ts`（新建）

RED：先冻结严格 UUID session 路径参数；再对四组精确路径覆盖 401、422、404、分页、Owner 隔离、503 零消息和 Fake Provider 201 双消息；断言 capabilities 在生产无 Provider 时为 `NOT_CONFIGURED`。

GREEN：注册 routes；`buildApp` 接收可选 `agentProvider` 仅用于组合根/测试注入；生产调用保持 undefined。

验收：没有 `/generate`；没有生产假回复；503 code 精确为 `AGENT_PROVIDER_NOT_CONFIGURED`。

验证：

```text
npm test --workspace @ev/core -- agent-api.test.ts
npm run typecheck --workspace @ev/core
```

独立提交：`feat(core): expose authenticated agent conversation api`

### Task 6：修复 Today 101+ 统计截断

候选文件（4）：

- `apps/core/src/modules/tasks/repository.ts`
- `apps/core/src/modules/tasks/service.ts`
- `apps/core/src/modules/today/routes.ts`
- `apps/core/tests/today.test.ts`

RED：创建 101 条同日任务和一条其他日期任务，断言 Today 的 tasks、优先级和原因基于全部 101 条；现实现应只看到 100 条。

GREEN：增加 Owner+日期的完整 Today 查询或独立聚合路径；不通过循环分页拼装，不改变普通 Task 列表分页。

验收：第 101 条参与状态计算；其他 Owner/日期仍被排除。

验证：

```text
npm test --workspace @ev/core -- today.test.ts tasks.test.ts
```

独立提交：`fix(core): calculate today from the complete task set`

### Task 7：返回并消费 Task 409 的服务端最新版本

候选文件（3）：

- `apps/core/src/modules/tasks/service.ts`
- `apps/core/tests/tasks.test.ts`
- `apps/web/src/lib/core-client.ts`

RED：Core  stale PATCH 必须返回 `details.currentTask`；Core client 必须在 `CoreClientError.details` 保留已校验 details。

GREEN：冲突函数接收当前 Task；数据库竞争导致第二次 update 失败时重新读取最新 Task；Web error 不丢 details。

验收：错误响应不含 Owner、Cookie 或密码；现有 409 code/message 保持兼容。

验证：

```text
npm test --workspace @ev/core -- tasks.test.ts
npm test --workspace @ev/web -- core-client
npm run typecheck
```

独立提交：`fix(tasks): preserve latest task on version conflict`

### Task 8：收紧 BFF loopback 与 Cookie 边界

候选文件（2）：

- `apps/web/src/app/api/core/[...path]/route.ts`
- `apps/web/tests/core-proxy.test.ts`

RED：非 loopback HTTP(S)、含混淆 hostname 的 origin 被拒绝；`127.0.0.1`、`localhost`、`[::1]` 允许；上游只收到 `ev_session`，不收到其他 Cookie。

GREEN：配置解析后按 hostname 白名单拒绝；使用安全 Cookie 解析/重建最小 header，不原样复制 `cookie`。

验收：路径、query、set-cookie 现有行为不回归；配置错误返回 `CORE_CONFIGURATION_ERROR`。

验证：

```text
npm test --workspace @ev/web -- core-proxy.test.ts
npm run typecheck --workspace @ev/web
```

独立提交：`fix(web): constrain core proxy to loopback session traffic`

### Task 9：实现认证启动解析与单 Owner 页面门禁

候选文件（5）：

- `apps/web/src/app/page.tsx`
- `apps/web/src/components/auth/auth-bootstrap.tsx`（新建）
- `apps/web/src/components/auth/auth-shell.tsx`
- `apps/web/src/components/auth/auth-form.tsx`
- `apps/web/tests/auth-bootstrap.test.tsx`（新建）

RED：覆盖 `/` 三状态、已有 Owner 访问 setup、未初始化访问 login、已登录访问认证页、Core unavailable 留页重试，以及 409/429/错误密码的准确文案。

GREEN：在表单出现前运行统一 resolver；复用 setup-status/session 和现有 `requestCore`，不引入认证状态库。

验收：不存在“预览账号”；已有 Owner 看不到第二账号表单；无重定向循环；不提供重置/清库。

验证：

```text
npm test --workspace @ev/web -- auth-bootstrap.test.tsx auth-form.test.tsx
npm run typecheck --workspace @ev/web
```

独立提交：`fix(auth): resolve owner startup state before rendering forms`

### Task 10：建立共享 authenticated Dashboard 路由外壳

候选文件（7；其中 Today page 是路径移动，额外两处用于复用认证解析并移除旧的嵌套 Shell）：

- `apps/web/src/components/auth/auth-bootstrap.tsx`
- `apps/web/src/app/(dashboard)/layout.tsx`（新建）
- `apps/web/src/app/(dashboard)/today/page.tsx`（由现有页面移动）
- `apps/web/src/components/shell/app-shell.tsx`
- `apps/web/src/components/today/today-dashboard.tsx`
- `apps/web/tests/app-shell.test.tsx`
- `apps/web/src/app/dashboard.css`

RED：组件/路由测试断言真实 href、pathname 活动项、401→login、未初始化→setup，以及 1024px 图标控件仍名为“今天/任务/Agent/退出”。

GREEN：复用统一 startup resolver 增加 `dashboard` 入口，由 route group 的唯一 layout 持有 `AppShell`；`AppShell` 自己管理登出并使用 Next `Link` 与 `usePathname`，Today 只渲染 canvas 内容，不再嵌套第二层 Shell；移除锚点 state 和 DOM 查找；桌面右侧 provider-neutral agent slot 保持可见，Task 14 再替换为完整上下文面板。

验收：`/today` 可直接访问/刷新；Dashboard 入口仍按 setup-status → session 判定 `/setup`、`/login` 或显示 Shell；活动态只由 pathname 决定；键盘与可访问名称不依赖隐藏文字；DOM 中只有一套主导航、移动导航和右侧 rail。

验证：

```text
npm test --workspace @ev/web -- app-shell.test.tsx
npm run typecheck --workspace @ev/web
npm run build --workspace @ev/web
```

独立提交：`feat(web): add authenticated dashboard route shell`

### Task 11：实现 Tasks 分页、筛选与只读列表状态

候选文件（5）：

- `apps/web/src/app/(dashboard)/tasks/page.tsx`（新建）
- `apps/web/src/components/tasks/tasks-workspace.tsx`（新建）
- `apps/web/src/components/tasks/task-filters.tsx`（新建）
- `apps/web/tests/tasks-workspace.test.tsx`（新建）
- `apps/web/src/app/dashboard.css`

RED：断言分页 URL/请求、领域/状态/全部/今天/未来/未安排筛选、空状态、错误和重新加载。

GREEN：通过现有 BFF 调 Task API；筛选变化回到 page 1；每页使用服务端 pagination，不在浏览器加载全部再过滤。

验收：未来和未安排任务可见；刷新页面保留可分享的查询参数；无全局状态库。

验证：

```text
npm test --workspace @ev/web -- tasks-workspace.test.tsx
npm run typecheck --workspace @ev/web
```

独立提交：`feat(web): add paginated task workspace`

#### Task 11A：执行 Task 查询筛选（审查新增 P1）

候选文件（2）：

- `apps/core/src/modules/tasks/repository.ts`
- `apps/core/tests/tasks.test.ts`

RED：用隔离数据库证明 `area`、`status`、精确日期、FUTURE、UNDATED 与组合筛选当前返回错误 rows/total；证明分页必须在筛选后发生。

GREEN：从已校验的 `TaskListQuery` 静态构造参数化 AND 谓词；Owner 永远是首要条件；rows 与 count 复用完全相同的谓词和 bindings；FUTURE 为严格晚于 `referenceDate` 且自然排除 null/当天，UNDATED 为 `target_date IS NULL`。不改变排序、普通分页、Today `listForDate` 或公共契约。

验收：每个筛选、组合筛选、Owner 隔离及筛选后 `total/totalPages` 均有集成证据；不拼接用户原始 SQL。

验证：

```text
npm test --workspace @ev/core -- tasks.test.ts today.test.ts
npm run typecheck --workspace @ev/core
git diff --check
```

独立提交：`fix(core): apply task filters before pagination`

### Task 12：完成 Tasks 创建、编辑和状态操作

候选文件（5）：

- `apps/web/src/components/tasks/tasks-workspace.tsx`
- `apps/web/src/components/tasks/task-editor.tsx`（新建）
- `apps/web/src/components/tasks/task-actions.tsx`（新建）
- `apps/web/tests/tasks-workspace.test.tsx`
- `apps/web/src/app/dashboard.css`

RED：创建、编辑、完成、延期、取消；409 后显示服务器最新内容且不覆盖；mutation 错误不丢列表；键盘焦点返回合理位置。

GREEN：复用 POST/PATCH；状态操作发送当前 version；409 用 `details.currentTask` 替换本地旧项并显示冲突提示。

验收：任务闭环完整；没有 delete、批量、拖拽或日历扩张；成功/失败状态可区分。

验证：同 Task 11，并运行 Core `tasks.test.ts`。

独立提交：`feat(web): complete versioned task management flows`

### Task 12R：修复 Task 12 反方审查中的 P1

候选文件仍限定为 Task 12 的五个文件：

- `apps/web/src/components/tasks/tasks-workspace.tsx`
- `apps/web/src/components/tasks/task-editor.tsx`
- `apps/web/src/components/tasks/task-actions.tsx`
- `apps/web/tests/tasks-workspace.test.tsx`
- `apps/web/src/app/dashboard.css`

RED：同版本列表刷新不清除其他编辑器草稿；`DONE`/`CANCELLED` 不出现状态写操作；筛选页 PATCH 成功后 GET 失败保留上一次完整 page 且 PATCH 恰好一次；编辑后任务移出筛选时标题获得焦点；错误 id 的成功/409 响应均不替换列表；移动控件至少 16px；200 字符标题不隐藏编辑控件；全仓 lint 可通过。

GREEN：编辑器按稳定 id/version 初始化；普通成功 mutation 等待完整 reload 后更新 page，冲突替换是唯一局部 reconciliation；显式终态动作矩阵；跨 reload 焦点 fallback；PATCH 资源身份关联；补齐 Task 12 新控件的移动字号和 flex min-width 约束。

边界：只修复 P1-04、P1-05、P1-10 至 P1-14；P2-01、P2-02、P3-01 继续记录，不在本切片实现。

验证：Task 12 聚焦测试、Core tasks、Web typecheck/build、全仓 lint、`git diff --check`。

独立提交：`fix(web): harden task mutation state transitions`

### Task 13：修复 Today 创建恢复与响应式长文本

候选文件（5）：

- `apps/web/src/components/today/today-dashboard.tsx`
- `apps/web/src/components/today/task-composer.tsx`
- `apps/web/src/components/today/status-overview.tsx`
- `apps/web/tests/today-dashboard.test.tsx`
- `apps/web/src/app/dashboard.css`

RED：POST 201 后 Today GET 失败时标题清空、只出现一次 POST、显示“已保存但刷新失败”；200 连续字符在任务/priority 表面不扩大文档宽度；移动输入计算字号至少 16px。

GREEN：把 create success 与 refresh success 分开；增加刷新恢复提示；补齐 `min-width:0`、`overflow-wrap:anywhere` 和移动 `1rem` 控件字号。

验收：重试只重取 Today，不重复创建；320px 无横向溢出；触摸目标仍至少 44px。

验证：

```text
npm test --workspace @ev/web -- today-dashboard.test.tsx
npm run typecheck --workspace @ev/web
```

独立提交：`fix(web): make today creation and mobile layout resilient`

### Task 13R：修复 Today 创建确认时序与移动溢出 P1

候选文件仍限定为 Task 13 的五个文件。

RED：POST schema-valid 后保持 refresh GET pending，断言 Composer 已清空、写请求恰好一次且刷新有独立状态；320px/390px 真实样式下 200 字符 Today 标题不得扩大 document width。

GREEN：POST 校验后立即返回 `saved`，后台刷新自行更新 snapshot/警告；移动 titleline/title 使用 stretch、`min-width:0`、`width/max-width:100%` 保持截断链。

边界：只修 P1-15/P1-16；P2-03 与既有 P2/P3 继续记录，真实浏览器门在 Task 16 固化。

验证：Today/Tasks 聚焦测试、Web typecheck/build、全仓 lint、`git diff --check`，并执行 320/390px Chromium overflow probe。

独立提交：`fix(web): separate today persistence from projection refresh`

### Task 14：实现 `/agent` 会话与未配置 UI

候选文件（5）：

- `apps/web/src/app/(dashboard)/agent/page.tsx`（新建）
- `apps/web/src/components/agent/agent-workspace.tsx`（新建）
- `apps/web/src/components/agent/agent-context-panel.tsx`（新建）
- `apps/web/tests/agent-workspace.test.tsx`（新建）
- `apps/web/src/app/dashboard.css`

RED：未配置 capability 时输入/发送禁用且说明连接 API；session 创建/分页、message 加载；Fake API 返回双消息后渲染；401/404/503 独立状态；UI 不生成本地假 assistant。

GREEN：页面只消费四组 API；桌面 context panel 继续占右栏，移动 `/agent` 显示完整会话/消息/上下文/输入布局。

验收：生产未配置状态清楚且可用；不存在 DeepSeek/Codex SDK、Key 表单、streaming 或 tool UI。

验证：

```text
npm test --workspace @ev/web -- agent-workspace.test.tsx
npm run typecheck --workspace @ev/web
```

独立提交：`feat(web): add provider-neutral agent workspace`

### Task 14R：修复 Agent 会话一致性与焦点 P1

候选文件仍限定为 Task 14 的五个文件。

RED：错误 sessionId 历史不提交；首次历史 pending 时输入/发送禁用；创建会话使旧 list GET 失效；page 2 失败保留 page 1 与可用重试；发送成功在启用后恢复输入焦点。

GREEN：消息页增加资源身份关联；写入门要求可信消息页已加载；创建时 abort/invalidate list 并刷新 page 1；requested/displayed page 分离；按来源会话保存 post-send focus intent。

边界：只修 P1-17 至 P1-21；P2-04 多会话 draft/error 状态机只记录，不新增全局 store。

验证：Agent Web/Core 聚焦测试、Web typecheck/build、全仓 lint、`git diff --check`，并执行 READY/NOT_CONFIGURED 与移动浏览器探针。

独立提交：`fix(web): preserve agent conversation consistency`

### Task 14R2：修复 Agent 创建投影与分页失败导航 P1

候选文件（2）：

- `apps/web/src/components/agent/agent-workspace.tsx`
- `apps/web/tests/agent-workspace.test.tsx`

RED（先在 `agent-workspace.test.tsx` 写失败回归）：(1) 创建成功后让 schema-valid page 1 遗漏 created id，断言该会话只出现一次、仍为选中项，并把本地投影的 total/totalPages 与该乐观项一致；再让 page 1 包含该 id，断言 overlay 清除且不重复。(2) page 2 失败且仍显示 page 1 后，分别点击每个 enabled 的“下一页会话”和“重新加载会话”；每一次都断言精确 `/agent/sessions?page=2&pageSize=20` GET 计数加一，并保留 page 1 的内容和页码。

GREEN：为 created session 建立只作用于 page 1 的 pending overlay；只要服务端 page 1 未包含该 id，就去重合并该项并按本地投影同步 total/totalPages，包含后立即清除并采用服务端元数据。保留页码为请求目标，将 `sessionReloadKey` 作为独立 attempt identity；目标页与当前请求页相同时递增该 key，使导航或显式重试均能重新派发。

边界：只修 P1-22 与 P1-23；不增加 API、CSS、会话删除/归档或全局状态管理；P2-04 多会话 draft/error store 继续延期。

验证（从仓库根目录执行）：

```text
npm test --workspace @ev/web -- agent-workspace.test.tsx
npm run typecheck --workspace @ev/web
npm run build --workspace @ev/web
npm run lint
git diff --check
```

Chromium：注入 page 2 失败后逐个点击全部 enabled 分页/重试控件，核对每次精确 page 2 GET 与 page 1 保留；复测 READY/NOT_CONFIGURED、发送后焦点及 320/390px 长文本。

独立提交：`fix(web): preserve created agent sessions and retry pagination`

### Task 15：隔离 Playwright 服务端口与测试数据

候选文件（2）：

- `apps/web/playwright.config.ts`
- `apps/web/tests/playwright-config.test.ts`（新建或并入现有配置测试）

RED：配置不得使用 3000/4311；测试数据目录必须是新建临时/明确 E2E 根；拒绝工作区外递归删除；`reuseExistingServer=false`。

GREEN：使用专用 Web/Core 端口和唯一 `EV_DATA_DIR`，Core URL 指向专用 loopback；清理只针对已校验 E2E 目录。

验收：运行 E2E 不需要停止用户预览，也不读写用户数据库。

验证：

```text
npm test --workspace @ev/web -- playwright-config
npm run test:e2e --workspace @ev/web -- --list
```

独立提交：`test(e2e): isolate dashboard servers and data`

### Task 16：建立四视口真实路由 E2E 矩阵

候选文件（3）：

- `apps/web/e2e/owner-task-dashboard.spec.ts`
- `apps/web/e2e/dashboard-routes.spec.ts`（新建）
- `apps/web/e2e/agent-unconfigured.spec.ts`（新建）

RED：先写 `/` 状态、真实 `/tasks`/`/agent`、直接加载、刷新、前进/后退、任务 CRUD/409、Agent 503、长标题、键盘、touch、无溢出与零 console warning/error 断言。

GREEN：只补必要的稳定定位和独立上下文；每个视口使用唯一 Owner/Task，移动上下文启用 touch。

验收：1440×900、1024×768、390×844、320×800 全部通过；网络断言证明没有假 Agent 成功响应。

验证：

```text
npm run test:e2e
```

独立提交：`test(e2e): cover desktop and touch dashboard routes`

### Task 16R：补齐 Terra 复审确认的 E2E 测试门

候选文件仍限定为 Task 16 的三个 E2E 文件：

- `apps/web/e2e/owner-task-dashboard.spec.ts`
- `apps/web/e2e/dashboard-routes.spec.ts`
- `apps/web/e2e/agent-unconfigured.spec.ts`

RED：先写三项失败回归：(1) 四视口路由测试在 first navigation 前收集 console error/warning，覆盖 setup/login 到最后路由，并在结束时精确断言完整集合为零；(2) 未配置 Agent 503/no-fake-response 场景也从导航前收集并精确断言零 console error/warning；(3) 390×844 与 320×800 两个 touch viewport 都断言无横向溢出、适用触摸目标至少 44px、相关输入/选择控件计算字号至少 16px。

GREEN：只在上述三个 E2E 文件把 console listener 前移至导航前、将未配置 Agent 纳入同一零 console 门，并把 touch 的 computed-style/overflow 断言应用到两个移动视口；保持既有独立 Owner/Task、503 网络语义和无假响应断言。

边界：只关闭 P1-24、P1-25、P1-26；不改 apps、ledger 或 `next-env.d.ts`，不扩张生产行为或 E2E 候选文件。“不支持 delete”是既有冻结边界；Terra 复审中缺少删除能力不构成问题，不记录为 P1。

验证：运行 Task 16 的真实 Playwright 矩阵，确认四视口完整路径与未配置 Agent 场景均零 console error/warning；确认 390×844、320×800 均通过 44px、16px 与无横向溢出断言；执行 `git diff --check`。

独立提交：`test(e2e): close task 16 review gates`

### Task 16R2a：认证状态探测契约与 Core

候选文件（5）：

- `packages/contracts/src/auth.ts`
- `packages/contracts/tests/contracts.test.ts`
- `apps/core/src/modules/auth/service.ts`
- `apps/core/src/modules/auth/routes.ts`
- `apps/core/tests/auth.test.ts`

RED：先在 contracts 测试定义唯一的 session response discriminated union：`{ data: { authenticated: true, owner } }` 与 `{ data: { authenticated: false } }` 都可解析，false 分支不得携带 owner。再在 Core auth 测试中建立已初始化但没有 cookie 的 `GET /v1/auth/session` 回归（200、`authenticated:false`）、有效旧 Owner session（200、`authenticated:true` 与原 Owner）、logout 后同一路径（200、`authenticated:false`）；同时精确断言匿名 Task 与 Agent 受保护资源仍为 401。

GREEN：将 session response 固化为上述 discriminated `authenticated` 契约；setup/login 成功响应显式返回 `authenticated:true` 以继续满足同一契约。仅让 `GET /v1/auth/session` 直接通过 AuthService 读取可选 Owner 并返回 false/true 200，其他受保护 route 保持既有 guard 与 401 行为；不得把“无会话”扩展为对 Task、Agent 或其他资源的匿名访问。

边界：最多五个候选文件；不改 Web、E2E 或 `apps/web/next-env.d.ts`，不改 Owner、cookie、logout 撤销、Task/Agent 授权边界。

验证：

```text
npm test --workspace @ev/contracts -- contracts.test.ts
npm test --workspace @ev/core -- auth.test.ts
npm run typecheck --workspace @ev/contracts
npm run typecheck --workspace @ev/core
git diff --check
```

独立提交：`fix(auth): make session probe explicit`

### Task 16R2b：Web 启动分支与 E2E 回归

候选文件（最多 5 个变更单元）：

- `apps/web/src/components/auth/auth-bootstrap.tsx`
- Web `auth-bootstrap` / `app-shell` 单测
- `apps/web/e2e/owner-task-dashboard.spec.ts`
- `apps/web/e2e/dashboard-routes.spec.ts`
- `apps/web/e2e/agent-unconfigured.spec.ts`

RED：先把 auth-bootstrap 与 app-shell 的启动回归改为消费 200 的 `authenticated:false`，验证已初始化且未登录会进入 login、已登录的 login entry 仍进入 `/today`，并让 setup/login 的重复探测不产生 console error。再在三个既有 E2E spec 从首次导航开始收集 console error/warning，覆盖 setup/login、认证后路由及未配置 Agent 503/no-fake-response 路径；完整集合必须为零。

GREEN：启动解析只按 `authenticated` 判别会话，false 是正常无会话而不是 `CoreClientError`；保留 `needsSetup` 优先级、Core 不可用错误态以及“已登录 login → /today”。更新三个既有 E2E spec 的断言以消费新的 200 probe 语义并运行全套矩阵，不以忽略 401、过滤 console 或放宽零 console 门取代修复。

边界：只关闭 P1-27；不改 `apps/web/next-env.d.ts`，不改变 Task/Agent 的 401 受保护语义，不新增 E2E spec、全局状态或生产权限例外。

验证：

```text
npm test --workspace @ev/web -- auth-bootstrap.test.tsx app-shell.test.tsx
npm run typecheck --workspace @ev/web
npm run test:e2e
git diff --check
```

独立提交：`test(e2e): regress explicit auth session probe`

### Task 16R3：补齐任务全生命周期 console 诊断门 P1

候选文件（最多 2 个）：

- `apps/web/e2e/owner-task-dashboard.spec.ts`
- `apps/web/tests/owner-task-dashboard-console-gate.test.ts`（新建，唯一的门禁测试文件）

RED：先在唯一门禁测试中固定诊断收集契约：`console.warning`、`console.error` 与 `pageerror` 均在首次 `page.goto('/')` 前注册，且不得在认证完成后才开始收集。再把 owner Task E2E 的认证、创建、编辑、409、完成、延期、取消、logout 和 logout 后受保护 Task 401 路径置于同一收集窗口；当前实现因仅在认证后收集 console error 而失败。所有语义断言完成后，三类诊断集合都必须精确为空。

GREEN：只在 `owner-task-dashboard.spec.ts` 建立可由该门禁测试验证的导航前诊断收集，并同时记录 console warning/error 与 `pageerror`；不忽略、过滤或降级任何诊断。保持既有任务写入、409、终态、logout 和 401 语义断言；在最后一个 logout/401 断言之后精确验证 warning、error、pageerror 集合均为空。

边界：只关闭 P1-28；最多触及上述一个 E2E spec 和一个针对门禁的测试文件。不得改生产代码、认证/Task 契约、Playwright 配置、`apps/web/next-env.d.ts`、`pnpm-lock.yaml` 或 `pnpm-workspace.yaml`；不新增第三个测试文件，不提前勾选最终门禁。

验证：先运行门禁测试得到 RED，再在 GREEN 后运行该门禁测试与 `owner-task-dashboard.spec.ts` 定向 E2E；最后运行完整 `npm run test:e2e` 回归，并执行 `git diff --check`。

独立提交：`test(e2e): guard task lifecycle console diagnostics`

### Task 17：原子统一全部 workspace 版本

候选文件（6，版本集合的唯一例外；拆开会造成内部依赖短暂不一致）：

- `package.json`
- `apps/core/package.json`
- `apps/web/package.json`
- `packages/contracts/package.json`
- `packages/domain/package.json`
- `package-lock.json`

RED：运行只读版本一致性脚本，断言五个 package version 与所有 `@ev/*` 内部依赖均为 `0.2.0`；当前应因 `0.1.0` 失败。

GREEN：在一个原子变更中把根、Core、Web、contracts、domain 与内部依赖推进到 `0.2.0`，更新 lockfile；不创建 Git 标签。

验收：lockfile 与所有 workspace package 一致，不出现 0.1/0.2 混用。

验证：

```text
npm test
npm run typecheck
```

独立提交：`chore(release): align dashboard runtime version`

### Task 18：同步运行时健康信息、README 与 CHANGELOG

候选文件（4）：

- `apps/core/src/modules/health/routes.ts`
- `apps/core/tests/health.test.ts`
- `README.md`
- `CHANGELOG.md`（新建）

RED：健康接口测试先期望 `0.2.0`；当前硬编码 `0.1.0` 应失败。人工/脚本检查 README 不得宣称真实 Provider、长期记忆或预设账号可用。

GREEN：健康接口报告 `0.2.0`；CHANGELOG 将 v0.1.0 标记为技术预览、v0.2.0 标记为待合并；README 增加三路由、独立数据目录和“无预设账号”说明。

验收：运行时健康信息、package 元数据和文档一致；不创建 `v0.2.0` tag。

验证：

```text
npm test --workspace @ev/core -- health.test.ts
npm run typecheck
git diff --check
```

独立提交：`docs: document v0.2 dashboard preview`

## 4. 每个任务的 Sol 复审清单

Sol 必须在新上下文中按以下顺序分类，而不是只给总体评价：

1. 契约误读：实现与冻结规格冲突；退回 Terra 修正。
2. 需修复：有效 P0/P1、安全、数据、Owner 隔离、回归或测试证据问题；退回 Terra 修正。
3. 接受的权衡：符合已写入规格的最小实现限制；记录但不扩张。
4. 噪声：无法复现、与范围无关或要求被明确拒绝的建议；写明拒绝理由。

复审重点：生产无 Fake Provider、503 前零写入、migration 001 未变、BFF 只连 loopback/只转发 `ev_session`、Task 409 保留最新实体、E2E 不碰用户数据。

## 5. 集成与最终门禁

所有切片合入 `codex/v0.2.0` 后，主 Agent 顺序执行：

```text
npm test
npm run typecheck
npm run lint
npm run build
npm run test:e2e
git diff --check
npm audit --omit=dev
code-intel "<repo>" --mode lite
Sentrux session_end
```

此外需要：

- 用全新临时数据库单独重跑 001→002 升级测试；
- 在真实浏览器复核四视口、路径历史、Task 闭环、Agent 未配置、焦点、触摸目标、长标题与 console/network；
- Sol 做最终全仓审查，所有有效 P0/P1 清零；
- 主 Agent 编写 `docs/releases/2026-08-10-v0.2.0-acceptance.md`，记录环境、矩阵、证据、P2/P3、回滚和无预设账号；
- 推送 `codex/v0.2.0`，创建目标为 `main` 的 ready-for-review PR，不自动合并、不打 `v0.2.0` 标签；
- 使用全新独立预览 `EV_DATA_DIR` 在 3000/4311 启动，确认 `http://127.0.0.1:3000/setup` 可由用户自行创建唯一测试账号。

## 6. 停止条件

- 任一迁移数据丢失、认证/Owner 越界、非 loopback 凭据转发、Agent 503 后写入消息、有效 P0/P1 或高危/严重运行时漏洞：停止 PR。
- 需要新外部服务、真实 Key、破坏性迁移或范围外产品决策：停止并请求用户批准。
- 纯开发依赖漏洞、P2/P3 或有明确证据的接受权衡可进入验收文档，但不得被伪装成已修复。
